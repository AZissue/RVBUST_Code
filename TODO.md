# CRM_New 待办清单（修订版）

**日期**：2026-04-26
**状态**：本地代码已恢复稳定，逐项推进

---

## 待办优先级

### 1. 🔧 工单编号生成优化（最简单，10 min）
- **当前**：`'TK' + Date.now().slice(-6)` 极小概率重复
- **目标**：`TK` + 年月日 + 3位随机数（如 `TK260426123`）
- **文件**：`project/index.html`（`submitNewTicket`）
- **风险**：低

### 2. 🔒 密码加密（简单，20 min）
- **当前**：明文存储 `admin123`、`123456`
- **目标**：bcrypt 哈希，登录时 compare 验证
- **文件**：`server/server.js`（login 路由）、`server/database.js`（seedData）
- **注意**：修改后 db.json 中的旧明文密码会失效，需要重新生成 seed 数据
- **风险**：中（影响登录）

### 3. 📤 数据导出/备份（中等，30 min）
- **目标**：admin/stats 页面增加导出按钮，支持 JSON/CSV
- **后端**：新增 `/api/export/tickets`、 `/api/export/customers`
- **前端**：4 个导出按钮
- **风险**：低

### 4. 💬 客户互动历史（中等，40 min）
- **目标**：客户详情页新增「互动历史」标签页
- **后端**：`/api/customers/:id/interactions` CRUD
- **前端**：时间线展示（电话/邮件/拜访/会议/其他）
- **风险**：低

### 5. 📄 分页加载（中等，40 min）
- **目标**：工单/客户列表后端分页 + 前端分页控件
- **后端**：`page` + `limit` 查询参数
- **前端**：上一页/下一页按钮 + 页码信息
- **风险**：中（影响列表渲染逻辑）

### 6. 🖼️ 附件文件存储（较复杂，60 min）
- **当前**：图片 Base64 内联存储，db.json 容易膨胀
- **目标**：multer 上传到 `uploads/` 目录，db.json 只存路径
- **风险**：高（涉及前后端文件上传/显示逻辑）

### 7. 🚀 部署到服务器（较复杂，60 min）
- **目标**：部署到 120.55.245.72:38000
- **风险**：低（上次已成功部署，只需重新上传）

---

## 规则

- **每改一项 → 本地测试通过 → 再改下一项**
- **遇到报错立即停止，修好了再继续**
- **不批量修改，一次只做一件事**

---

## 当前基线

- 端口：3000（本地）
- 账号：`admin` / `admin123`（明文）
- 文件状态：`project/index.html` 已清 BOM，`server.js` 和 `database.js` 是原始稳定版本

---

*清单创建：2026-04-26 16:52*
