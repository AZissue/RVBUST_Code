# 会话提示词

> git init 已完成（AICode 与 D:\MyCode\MyHandEyeTools 均为 git 仓库，AICode 根为唯一
> 权威仓库）。本地提交照常；push 只在你说「推送」时进行。

## 下次会话启用（每次新会话开头粘贴）

```text
新会话启用：请先读取 AGENTS.md 并遵守其中的规则。然后依次：
1) 读取 STATUS.md 顶部的「交接块」；2) 读取 PLAN.md 的当前阶段与回退方案；
3) 读取 PROJECT.md 中与当前任务相关的约束。最后复述你对当前进度和下一步的
理解，确认无误后直接执行。若发现文档与代码矛盾，停下说明再继续。
```

## 会话收尾（每次结束前粘贴，或按 AGENTS.md 会话仪式执行）

```text
会话收尾：请按以下顺序收尾：
1. 更新 STATUS.md：刷新交接块（当前状态、下一步、风险），把本次完成内容移入
   「已完成」，记录新发现的 BUG、失败尝试及原因；
2. 检查 PROJECT.md / PLAN.md 是否因本次工作需要同步修改（需求变更、技术决策、
   阶段完成），需要则一并更新；
3. 运行两处工作区构建与测试，确认无回归；
4. 本地提交（Conventional Commits，scope=handeye-tools）；不 push，
   除非我明确说「推送」。
全程禁止破坏性命令，除非用户明确要求。
```

## 提交触发（本地提交照常；推送需你明确说「推送」）

```text
推送：请把当前分支推送到 GitHub：
1. 先 git pull --rebase 到最新；
2. 检查 git status，确认无密钥/大文件/构建产物；
3. 运行构建+单测全绿后 push。
```
