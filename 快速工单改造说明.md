# 快速工单与个人工作视图

## 本次范围

- 仪表盘快速记录改为规则解析工单，解析和相似检查不创建业务记录；确认后复用 `POST /tickets`。
- `/my-work` 使用 `GET /tickets?mine=1`，后端限定 `assigneeId = 当前用户 ID`，不包含仅创建或协作的工单。
- 工单详情支持修改负责人；已有状态流转、客户/设备、工作记录、报表保留。
- 左侧移除工作事项导航，个人工作页提供“历史事项”入口。原工作事项及日志仍保留，可继续处理内部历史任务，也可显式转换客户问题。
- 现有工单无百分比进度字段，因此个人工单列表不显示伪造进度。

## 文件与职责

| 文件 | 变更 |
| --- | --- |
| apps/api/src/tickets/quick-input.parser.ts | 可替换的 QuickInputParser、规则解析、客户/人员匹配、工单相似度 |
| apps/api/src/tickets/quick-tickets.service.ts | 读取真实候选、同客户同权限相似查询、确认更新 |
| apps/api/src/tickets/dto/quick-ticket.dto.ts | 解析、相似查询、更新、历史转换 DTO |
| apps/api/src/tickets/tickets.controller.ts、tickets.module.ts | 新 API 与解析器依赖注入 |
| apps/api/src/tickets/tickets.service.ts、dto/ticket.dto.ts | 我的工单过滤、关联校验、幂等创建、原文、负责人历史、转换 |
| apps/api/src/dashboard/dashboard.service.ts | 个人工单统计，保留原事实报表 |
| apps/api/src/workitems/workitems.service.ts | 已转换事项锁定，避免双数据源编辑 |
| apps/api/prisma/schema.prisma、migrations/202609040001_quick_ticket/migration.sql | 仅增加可空字段、索引、外键 |
| apps/web/src/components/QuickTicketInput.tsx | 可编辑确认表单、客户创建、候选选择、相似工单操作 |
| apps/web/src/components/LegacyWorkItemConversion.tsx | 历史事项显式转换入口 |
| apps/web/src/pages/TicketsPage.tsx | 个人工单列表、统计筛选、负责人编辑 |
| apps/web/src/pages/DashboardPage.tsx | 统一个人工单工作台与快速入口 |
| apps/web/src/pages/WorkItemsPage.tsx | 历史入口与转换后工单跳转 |
| apps/web/src/App.tsx、components/AppShell.tsx | 路由与导航调整 |
| apps/web/src/hooks/useRemote.ts、lib/api.ts | 工单/工作台变更通知、跨标签通知、重新聚焦刷新、15 秒轮询 |
| apps/web/src/quick-ticket.css、main.tsx、types.ts | 延续现有主题、布局与数据类型 |
| apps/web/vite.config.ts、playwright.config.ts | 可配置测试端口，避免影响正式服务 |
| apps/api/src/tickets/quick-input.parser.spec.ts、apps/api/test/quick-ticket.e2e-spec.ts、e2e/quick-ticket.spec.ts | 单元、真实数据库 API、浏览器回归 |

## 数据与迁移

新增 `tickets.raw_text` 保存完整输入；`tickets.request_key` 唯一索引用于确认创建幂等重试。

新增 `work_items.converted_ticket_id`，唯一且外键关联工单；不删除、重命名或清空任何原表。

历史转换在事务中锁定原事项，重复提交返回同一工单。复制标题、描述、分类、负责人、协作人、优先级、计划日期与状态；事件保存原事项 ID、原创建时间、原进度、原状态和标签。原事项保留并锁定；关联日志保留 workItemId，同时补充 ticketId。已有不同工单或客户的日志阻止转换，不强行覆盖。

转换映射：待开始 -> 待处理；进行中 -> 处理中；等待反馈 -> 等待客户；已完成 -> 已解决；已取消 -> 已关闭。转换后可按真实情况调整工单状态。

本机改造前正式数据：1 张工单、2 条工作事项、1 条工作记录。两条工作事项均无客户，没有自动转换。原始数据库备份保存在工作区 outputs/before-quick-ticket-20260904.dump，不纳入 Git。

部署只执行 `npm run prisma:deploy -w apps/api` 和 `npm run prisma:generate -w apps/api`，不运行 reset、不执行正式环境 seed。

## 解析与匹配规则

1. 优先级复用 LOW/MEDIUM/HIGH/URGENT。紧急、非常紧急、停线、产线停机、马上处理为紧急；高优先级、优先处理、比较急为高；未注明默认普通。停机、2D正常等事实保留在问题文本。
2. 负责人优先识别“负责人/指定负责人/指派给/交给”；其次现有姓名、账号及“小张/张工”等姓氏候选。未填写默认当前用户；填写但无法匹配时不默认替代。
3. 客户从句首、型号前或分隔符前提取，去除客户、公司、有限公司、科技、机器人等词进行归一化。完全匹配、归一化相等、包含匹配、Levenshtein 编辑距离共同评分。
4. 客户候选最低分 0.58，自动选中要求 >= 0.85 且领先第二名至少 0.12；人员最低分 0.6，自动选中要求 >= 0.8 且领先至少 0.12。歧义必须人工选择，评分为规则相似度，不是概率。
5. 去除已识别客户、人员及优先级标签后保留问题，标题进行有限清理并截为最多 64 字；确认表单可修改。原文单独保存。
6. 型号按字母加至少三位数字识别。只在确定客户下匹配设备，同型号多台时不自动选择设备实例。

## 相似工单与同步

相似查询仅检索当前用户可见且同客户的工单，归一化“连接不上/连接超时”等同义表达，结合编辑距离和字符二元组 Dice 重合度。文本占 78%、同型号 17%、未结束状态 5%，规则分数达到 40% 才提示，最多展示 6 条。

修改确认表单中的客户、问题或型号会重新检查相似工单；检查失败时禁用创建。用户可选更新现有工单或仍新建。更新会再次确认，只追加内部记录并更新负责人/优先级，保留原描述与状态，使用 updatedAt 乐观并发检查避免覆盖他人修改。

我的工作与工单没有复制或同步表。同页面写入后刷新工单查询，同浏览器标签用 BroadcastChannel 通知；其他会话在重新聚焦或最多 15 秒轮询后刷新。未实现跨设备 WebSocket/SSE 推送。

首页五项统计均限定当前负责人：今日待办为未设计划时间或计划在明天前的待处理工单；已完成统计以今天 resolvedAt 的已解决/已关闭工单为准。直接取消关闭而未解决的工单不算今日解决数。

## 测试隔离

测试配置使用 `schema=quick_ticket_test`，API 3002、Web 5174；正式数据为 public。测试套件设置了 schema 防护，不应直接针对正式 `.env` 运行。

验证命令：`npm test`、`npm run test:e2e -w apps/api`、`npm run build`。浏览器使用 WEB_PORT=5174、API_PORT=3002，并通过 `npm run test:e2e` 读取独立测试配置。

测试输入覆盖：

- 浙江智享机器人 M2600拍摄3D无点云 张伟 紧急
- 浙江智享 M2600连接不上 张伟 高优先级
- 工布 G52000 2D正常3D无点云 李四 普通
- 浙江智享机器人 M2600拍摄超时
- M2600无点云 张伟 紧急（缺客户，禁止直接创建）

同时覆盖客户简称、多候选、小张/张工、未知负责人、同义问题相似度、跨客户/跨权限隔离、幂等创建、追加记录及并发冲突、负责人转移、状态统计、历史转换、刷新持久化、原工作记录草稿流程、亮暗主题与移动布局。

## 验证结果（2026-09-04）

- 前后端生产构建通过，实际 D 盘运行目录也完成构建。
- 单元测试 18/18；PostgreSQL API 测试 19/19；Playwright 新旧流程 2/2。
- 桌面亮/暗主题、手机确认窗口已截图检查，修复个人工单列表网格挤压。
- 正式站登录后只读验收通过，客户/设备/工作记录/报表/历史事项页面可访问。
- 正式业务数据数量保持 1 张工单、2 条工作事项、1 条工作记录。测试使用独立 schema，未向正式库写入测试工单。
- 测试创建和转换仅在 quick_ticket_test schema 执行；正式库没有执行历史事项转换。

## 当前边界与注意事项

规则解析不能替代完整中文语义模型，结果必须确认；不依赖外部 AI 或 API Key。数据库未新增用户别名表，昵称仅为可解释的姓氏候选。

原日报/周报/月报入口与统计报表实现保持不变，本次不补造原项目尚未实现的独立报告功能。内部工作事实依然可以通过工作记录独立保存。
