# PROJECT.md — 项目定义与约束

> 最后更新：2026-08-25
> 本文件回答「做什么、为什么、什么不能动、怎样算完成」。
> 需求或约束变化时直接改本文件，不要在 STATUS.md 里只记一笔。

## 1. 项目目标

- 一句话目标：面向 RVC X1/X2 相机的 Windows 手眼标定数据采集桌面工具，完成
  「连接相机 → 采集 2D+3D → 标定板识别 → 数据保存/备份 → 辅助工具（测量/坐标转换/
  像素→3D/标定计算/机器人通信）」的现场工作流，以绿色免安装包交付。
- 背景与动机：现场手眼标定依赖官方软件采集数据再人工整理，抄写位姿易出错、
  流程割裂；本项目把采集、校验、计算、验证一体化，并降低目标机部署门槛。
- 目标用户 / 使用场景：现场操作人员（采集与保存数据）和技术人员（配置参数、
  标定计算、走点验证、故障排查）。

## 2. 当前需求（按确认时间记录）

| 日期 | 需求描述 | 状态 |
|---|---|---|
| 2026-08-24 | v1.0 全功能（连接/预览/采集/识别/保存/备份/日志/绿色打包） | 已确认（已入 main） |
| 2026-08-24 | 工具板块：欧氏距离、坐标转换/走点验证（矩阵+安装方式+7 种位姿格式+单位） | 已实现（阶段 1/3），坐标转换待 UI 实测 |
| 2026-08-24 | 阶段 4：2D 像素点 → 3D 点（在线点击+离线加载数据） | 已实现，待联测 |
| 2026-08-24 | 阶段 5：棋盘格标定识别 | 未开工 |
| 2026-08-24 | 阶段 6：相机精度好、机器人精度差时的标定优化 | 未开工（研究先行） |
| 2026-08-24 | 阶段 7：程序内手眼标定 + 残差报告 + 批量走点验证 | 已实现（批量走点验证待补），待联测 |
| 2026-08-24 | 阶段 8：机器人位姿自动读取 / 姿态引导 / 数据质检 / 板位姿可视化 | 机器人通信已实现；其余 3 项待确认 |
| 2026-08-24 | 阶段 9：数据管理与交付（会话浏览器/离线回放/报告/专属文件导出） | 未开工 |
| 2026-08-24 | 阶段 10：相机与场景工具（参数档案/2D 测量/2D 内参标定） | 未开工 |

## 3. 不可修改的核心功能（红线）

> 以下功能无论后续如何演进都不能破坏；改动相关代码前先对照此清单。

- [x] 识别回退链：同心圆 = 采集缓存 → RVC 原生 → HandEyeSDK；黑底白圆 = RVC
  TestAccuracy → HandEyeSDK。*为什么不能动：单一引擎识别失败率高，回退链保证现场可用。*
- [x] 质量告警不拦截：误差/点数不足只提示，不拒绝识别与保存。*为什么不能动：
  现场需要「先存后看」，拦截会卡住采集流程。*
- [x] 检测缓存：同帧同参数重复识别不重算（命中缓存按当前阈值重算告警）。
  *为什么不能动：重复点击必须瞬时响应。*
- [x] 3D 拾取异步化：Vis 同步命令绝不放 UI 线程；交互冷却 + 工作线程查询。
  *为什么不能动：同步调用可整机挂死。*
- [x] 3D 缩放深度自适应：滚轮补偿 1~30×，纯本地计算不调 Vis。*为什么不能动：
  深放大手感依赖本地补偿。*
- [x] 保存路径回退：配置路径不可用自动回退 `<exe>/data`（再退文档目录）并提示。
  *为什么不能动：目标机路径差异必须免配置可用。*
- [x] 卡片格式校验：中文/全角拒绝并提示，英文逗号/空格合法。*为什么不能动：
  脏数据直接毁掉标定结果。*
- [x] 绿色版免安装：VC 运行库 + RVC/HandEyeSDK/OSG DLL 随包；剔除 Debug/测试产物。
  *为什么不能动：目标机免装依赖是交付承诺。*
- [x] 日志双轨：runtime（技术/崩溃）+ app（操作）。*为什么不能动：故障排查
  必须能区分程序问题与操作问题。*
- [x] 会话目录精简：只保存 png/ply/txt；元数据写日志；备份在 backup/ 子目录。
  *为什么不能动：目录结构已被客户与后续工具依赖。*

## 4. 约束条件

- 技术约束：Windows 10/11 64 位；C++17；Qt 5.14.2 (msvc2017_64)；CMake +
  Visual Studio 2026；RVC SDK / HandEyeSDK / RVBUST Vis(OSG) / Qt5 Network。
- 环境约束：第三方 SDK 本地化（third_party 不入库）；双工作区（AICode 真源 +
  D:\MyCode\MyHandEyeTools 运行副本）；绿色包随带全部运行库。
- 流程约束：纯函数先单测；UI 用「操作步骤+预期结果」清单由用户手动验证；
  提交前两处构建+ctest 全绿；AICode 根 AGENTS.md / docs/GOVERNANCE.md 同时适用。
- 范围与时间：不设截止日期或里程碑，按阶段推进；范围以 PROJECT/PLAN 为准，
  需求变化先确认再改。

## 5. 非目标（防止范围蔓延）

- 坐标换算、标定板尺寸计算（2026-08-24 已删除）。
- 批量距离计算、位姿换算、3D 取点测距（2026-08-24 用户确认不做）。
- 自动化 UI 测试（pywinauto/进程内探针）；界面验证一律用手动清单。
- 依赖系统安装 VC++/RVC/HandEyeSDK 的交付方式（必须绿色免安装）。
- 若后续需求与上述非目标冲突，先提问确认再调整，不自行推翻。

## 6. 最终完成标准（必须可验证）

> 措辞采用可测描述（如「连拍中连续拖拽 5 秒后 3 秒内恢复响应、停止按钮可用」
> 替代「交互不卡顿」）；标准可能随后续需求更新或冲突，以最新确认版为准。

- [ ] 坐标转换：真实数据（FANUC，D:\日常支持数据\handeye\HandEyeData）标定集
  平均误差 ≤1.5mm、验证集 ≤1.2mm；7 种姿态格式与度/弧度、mm/m 可选；
  复制结果只复制数值。
- [ ] 像素→3D：拍照后点击 2D 图有效像素，3D 视图同步显示且「相机目标点」
  自动填入；离线会话（对齐数据免内参；线扫用内参投影）可复算同一 3D 点。
- [ ] 标定一体化：≥6 组（建议 15-20）数据一键计算，输出 4×4 矩阵+总平均误差+
  逐组误差并可复制；同一数据集与官方 HandEyeSDK 直接调用结果逐元素一致
  （|Δ|<1e-6）。
- [ ] 机器人通信：Modbus TCP 连接/读取位姿填入「机器人拍照位姿」卡片；
  失败有明确提示且不影响采集/保存/识别/3D 链路。
- [ ] 交互：连拍中 2D/3D 视图连续拖拽 5 秒后 3 秒内恢复响应、停止按钮可用；
  除相机采集时间外，点击拍照到数据显示的额外耗时 ≤1.5s。
- [ ] 绿色包：目标机免装 VC++/RVC/HandEyeSDK 启动通过；保存路径不可用自动回退。
- [ ] 回归：单测 100% 通过、构建无错误；识别/保存/3D/日志全流程回归通过。
- [ ] 收尾：`tools-panel` 分支合并回 main，README 状态更新为 v2.0，根索引同步。
- [ ] 阶段 5/6/9/10 的验收标准：待规格确认后补充到本清单。

## 7. 技术决策记录（ADR，永久保留，只增不改，不删除历史）

| 日期 | 决策 | 备选方案 | 理由 |
|---|---|---|---|
| 2026-08-24 | 双工作区：AICode 真源 + D:\MyCode\MyHandEyeTools 运行副本 | 单工作区 | 源码入库/发布与现场构建隔离，绿色包从副本产出 |
| 2026-08-24 | 纯函数先行（TDD） | UI 直接调 SDK | 可单测、防改崩采集/保存链路 |
| 2026-08-24 | 3D 拾取异步化 | UI 线程同步查询 | Vis 同步命令可整机挂死 |
| 2026-08-24 | 3D 缩放深度自适应（1~30× 本地补偿） | 固定 1~8× 补偿 | 深放大仍需流畅 |
| 2026-08-24 | 绿色版免安装 + 保存路径回退 | 依赖系统安装 | 现场免装、路径差异免配置 |
| 2026-08-24 | 双日志（runtime + app） | 单一日志 | 操作与崩溃分离排查 |
| 2026-08-24 | FANUC WPR 按 RVC 导出实测顺序（WPR=绕 Z,Y,X） | 按标准手册（W绕X） | 真实数据复算 0.78~0.91mm 验证 |
| 2026-08-24 | 像素→3D 主路径走 SDK 对应关系，内参仅离线备用 | 全部依赖内参反投影 | 对齐数据免内参；线扫用对应图/投影 |
| 2026-08-24 | 标定 staging：零填充命名 + 位姿文件规范化 | 直接传会话目录 | 避免 SDK 文件排序与记录顺序不一致 |
| 2026-08-24 | 机器人通信统一 Reader 接口 + Modbus 先行、默认关闭 | 各品牌直接实现 | UI 只依赖接口；隔离不影响现有链路 |
| 2026-08-24 | 本地提交照常、push 需明确 | 仅用户说「提交」才 commit | 每阶段本地提交便于回溯审计（用户确认保留） |
| 2026-08-25 | 文档体系按新模板重构为 4+1 份（AGENTS/PROJECT/PLAN/STATUS + SESSION_PROMPTS） | 维持原三份 | 会话自举、交接块、ADR 常驻、DoD/回退可执行 |

## 8. 变更记录

| 日期 | 变更内容 | 影响范围 | 修改人/会话 |
|---|---|---|---|
| 2026-08-24 | 建立 PROJECT/PLAN/STATUS 三份文档（阶段 2） | 全项目 | AI-codex |
| 2026-08-24 | v2.0 需求纳入阶段 4-10（17 项） | 全项目 | AI-codex/用户确认 |
| 2026-08-24 | 实现阶段 4/7/8（提交 4619dda） | src/tests/docs | AI-codex |
| 2026-08-25 | 按新模板重构文档体系，新增 AGENTS.md 与 SESSION_PROMPTS.md | 文档体系 | AI-codex/用户确认 |
