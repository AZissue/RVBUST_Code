# PROJECT.md — PointCloudSearch 项目目标与约束

最后更新：2026-08-20

## 一、项目目标

模块化点云查找 / 分析工具：桌面程序（节点式图形化流程编排）+ C++ SDK，面向 RVC
3D 相机客户，对标海康 VisionMaster / CloudCompare 的易用性与可扩展性。

核心能力：

1. 点云输入：PCD / PLY / XYZ / CSV（RVC 相机支持格式），支持单位换算、中文路径；
   内部统一 mm。
2. 查找能力：平面检测、Z 值高度筛选、半径 / 包围盒查找、2D 形状（圆 / 矩形）、
   3D 形状（球 / 圆柱 / 长方体）、空洞检测。
3. 节点化流程：功能自由组合（不固化流程）、类型化端口前置约束、方案保存 / 加载、
   逐节点耗时统计。
4. 可视化：3D 视窗（点云 + ROI 框选 + 几何叠加）、多视窗路由、2D 视窗（后续，
   配合 RVC 像素对齐）。
5. 深度学习（后续阶段）：语义分割 / 目标检测（YOLO / CNN 等，方案待定）。
6. SDK：稳定的 C++ SDK 接口 + 示例工程，客户可参照 GUI 用法集成。

## 二、当前需求（2026-08 汇总）

1. 点云格式 PCD/PLY/XYZ/CSV；下采样（体素 / 随机）可选算法，结果可直接输出或供后续使用。
2. 桌面程序 + SDK + 使用示例；模块化开发，数据读写解耦，问题可定位。
3. 各功能可单独使用，也可组合（图形化编程式）；输入输出统一为对象列表
   （`ObjectList`）；前置约束靠端口类型。
4. 实施顺序：先 3D 功能，再加 2D，最后深度学习语义。
5. RVC 相机点云与 2D 默认像素对齐；不对齐时需提供相机内外参。
6. 内部单位 mm，单位换算只在 IO 层；所有结果保留原始点索引。
7. 界面默认中文 + Dark 主题，可切换英文 / 浅色；蓝白磨砂玻璃风格。
8. 画布交互：拖拽连线、右键删除节点、右键断开连线、画布背景（网格 / 点阵 /
   纯色 / 自定义图片）。
9. 调试体验：选中节点时 3D 视窗显示上游输入点云；Box ROI 框选直观
   （左键拖动盒子、角手柄缩放、滚轮缩放视窗）。

## 三、不能修改的核心功能 / 架构约束

- 语言：C++（长期项目，RVC 相机 SDK 底层为 C++），C++20。
- 技术栈：VS2026 + CMake + PCL 1.13.0 + Qt 6.8.3 + VTK 9.4（OpenCV 4.7 待接入）
  + RVC SDK（`D:\Program Files\RVBUST\RVC\RVCSDK`）。
- 内部坐标单位恒为 **mm**；单位换算只发生在 IO 层。
- 所有结果保留原始点索引（`Region.indices` / `source_indices`）。
- 节点输入输出统一 `ObjectList`（`PointCloudObject` 列表）。
- 端口类型：cloud / region / any，类型不符禁止连线。
- 界面布局：左侧组件列表 | 画布 | 3D 视窗 | 右侧参数 + 点云属性 | 底部日志。
- 默认中文 + Dark 主题；切英文时才用英文。
- 测试先行：核心逻辑单测 + UI QtTest（`tests/app/test_node_flow.cpp`），
  `ctest` 全绿 + 应用冒烟通过后才能交付。

## 四、最终完成标准（验收）

1. MVP 完整链路：点云加载 → 任意筛选 / ROI → 平面 / 聚类 / 形状 → 保存，
   在 GUI 与 SDK 中均可调用。
2. 节点库齐备：IO（加载 / 保存）、预处理（移除无效点 / 体素 / 随机下采样 /
   Z 过滤）、ROI（Box ROI / ROI Crop）、分割（平面检测）、聚类（DBSCAN /
   欧几里得）、形状与空洞（圆 / 矩形 / 球 / 圆柱 / 长方体 / 空洞）、显示、保存。
3. 方案 JSON 保存 / 加载；CLI 支持 `--smoke / --demo / --autoquit`。
4. RVC 相机接入：采集节点、PointMap→点云、像素对齐 2D 显示、内外参可选。
5. 2D 识别与 3D 联动；深度学习语义分割 / 目标检测（YOLO / CNN）。
6. C++ SDK 稳定 API + 示例工程 + 文档。
7. 性能：千万级点云不卡 UI（异步执行引擎）。
8. 中英双语 + 明暗主题完整覆盖。

## 五、更新记录

- 2026-08-20：需求 7/8/9 已落地——画布交互（拖拽连线实时跟随、右键删除 / 断开、
  背景 4 种风格）、路径文件浏览、默认中文 + Dark、ROI 独立模块（RoiSelector）、
  选中节点显示上游输入点云。见 PLAN.md 阶段 1 / STATUS.md。
