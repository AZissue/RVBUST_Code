# PROJECT.md — 项目定义与约束

> 最后更新：2026-08-25
> 本文件回答“做什么、为什么、什么不能动、怎样算完成”。
> 需求或约束变化时直接改本文件，不要在 STATUS.md 里只记一笔。

## 1. 项目目标
- 一句话目标：模块化点云查找 / 分析桌面程序（节点式图形化流程编排）+ C++ SDK，
  面向 RVC 3D 相机客户。
- 背景与动机：客户需要对 RVC 相机点云做平面 / 形状 / 空洞查找、ROI 框选、
  聚类分割等分析；市面成熟方案（CloudCompare / RVC Manager）交互好但不可编程、
  不可组合，本产品提供可自由编排的节点化流程与可集成 SDK。
- 目标用户 / 使用场景：**现场操作人员**（GUI 拖拽编排流程，无需编程）与
  **技术开发人员**（SDK 集成、二次开发、参考示例代码）。

## 2. 当前需求（按确认时间记录）
| 日期 | 需求描述 | 状态 |
|---|---|---|
| 2026-08-18 | 支持 PCD / PLY / XYZ / CSV；体素 / 随机下采样可选算法，结果可输出或供后续使用 | 已确认 |
| 2026-08-18 | 桌面程序 + SDK + 使用示例；模块化开发，数据读写解耦，问题可定位 | 已确认 |
| 2026-08-18 | 功能可单独或组合使用（图形化编程式）；输入输出统一 `ObjectList`；端口类型前置约束 | 已确认 |
| 2026-08-18 | 实施顺序：先 3D 功能 → 再加 2D → 最后深度学习语义 | 已确认 |
| 2026-08-18 | RVC 相机点云与 2D 默认像素对齐；不对齐时需提供相机内外参 | 已确认 |
| 2026-08-18 | 内部单位 mm，单位换算做成输入 / 输出功能；结果保留原始点索引 | 已确认 |
| 2026-08-18 | 界面中英切换 + 白天 / 黑夜主题切换 | 已确认 |
| 2026-08-20 | 画布交互细节（连线跟随 / 右键删除 / 断开 / 背景风格）；默认中文 + Dark；ROI 独立模块与上游点云联动 | 已确认 |
| 2026-08-24 | Box ROI：拖移 / 缩放 / 旋转、重置包围盒（按输入点云 AABB）、显示降采样、保存节点“文件夹 + 文件名” | 已确认 |
| 2026-08-25 | 采用新项目管理文档模板（AGENTS / PROJECT / PLAN / STATUS + SESSION_PROMPTS）；git 本地提交照常、push 需明确 | 已确认 |
| 2026-08-25 | 批量语义统一约定：单文件 + 文件夹批量读取（全量 / 限量 / 逐帧）；一帧多盒（box_roi 输出 F×M 组）；批量保存（零填充命名）；多帧同窗显示预留（模板匹配等） | 已确认 |
| 2026-08-25 | 未来规划：批量输入 / 输出点坐标、平面、法向量、位姿等数据，程序内使用并支持保存到本地 | 规划中 |
| 2026-08-26 | 3D 视窗 ↔ 点云属性面板联动：选中节点在属性面板展示输入/输出逐帧；选帧驱动 3D 显示（多选多显示）；ROI 与 box_roi 只作用于选中帧（frame_filter） | 已确认 |

## 3. 不可修改的核心功能（红线）
> 以下功能无论后续如何演进都不能破坏，改动任何相关代码前先对照此清单：
- [ ] 语言 C++20（长期项目，RVC 相机 SDK 底层为 C++）——不能换语言或降标准。
- [ ] 内部坐标单位恒为 mm，换算只发生在 IO 层——算法层混用单位会静默出错。
- [ ] 所有结果保留原始点索引（`Region.indices` / `source_indices`）——客户需回溯到原文件点。
- [ ] 节点输入输出统一 `ObjectList`——所有节点组合的基础，破坏即破坏编排体系。
- [ ] 端口类型 cloud / region / any，类型不符禁止连线——错误前置到连线时而不是运行时。
- [ ] 界面布局（左组件 / 画布 / 3D / 右参数 + 属性 / 底日志）与默认中文 + Dark、蓝白磨砂玻璃主题——客户已确认的 UI 基调。
- [ ] 异步执行引擎（千万级点云不卡 UI）——桌面程序可用性底线。
- [ ] 测试先行：`ctest` 全绿 + 应用冒烟通过后才能交付——质量门槛。

## 4. 约束条件
- 技术约束：VS2026 + CMake + PCL 1.13.0 + Qt 6.8.3 + VTK 9.4（OpenCV 4.7 待接入）
  + RVC SDK（`D:\Program Files\RVBUST\RVC\RVCSDK`）。
- 环境约束：Windows 桌面（含远程桌面 / 核显场景，显示必须降采样）；中文路径必须支持。
- 流程约束：改代码前先查记忆库 / 参考仓库（见 AGENTS.md）；提交前 `ctest` 全绿
  + 冒烟通过；本地提交照常，push 需用户明确要求。
- 范围与时间：**不设硬性截止日期或里程碑**，按 PLAN 阶段依赖顺序推进；明确不做
  的事见第 5 节。

## 5. 非目标（防止范围蔓延）
- 不做浏览器 / Web / 移动端版本（桌面程序 + SDK）。
- 不做点云网格重建 / 编辑（PCL 网格工具不在产品范围，聚焦查找与分析）。
- 不提供多相机联合标定功能（内外参由用户提供）。
- 不承诺特定深度学习模型 / 框架与效果指标（YOLO / CNN 等方案后定）。
- 不开发数据标注 / 训练平台（训练流程不作为交付物，除非后续明确需求）。
- 不做通用 2D 图像处理（2D 仅用于 RVC 像素对齐与识别联动）。
> 以上非目标若与后续需求冲突，先向用户提问确认再调整本清单。

## 6. 最终完成标准（必须可验证，写成能检查的语句）
- [ ] 节点库齐备：IO（加载 / 保存）、预处理（移除无效点 / 体素 / 随机下采样 /
  Z 过滤）、ROI（Box ROI / ROI Crop）、分割（平面检测）、聚类（DBSCAN /
  欧几里得）、形状与空洞（圆 / 矩形 / 球 / 圆柱 / 长方体 / 空洞）、显示、保存。
- [ ] 方案 JSON 保存 / 加载；CLI `--smoke / --demo / --autoquit` 全部可用。
- [ ] RVC 相机接入：采集 → PointMap → mm 点云 → 保存链路在真机跑通；像素对齐
  2D 显示可用；内外参缺失时给出明确提示。
- [ ] 2D 识别与 3D 联动；深度学习语义分割 / 目标检测（方案确定后补充可测指标）。
- [ ] C++ SDK 稳定 API + 示例工程 + 集成文档，示例可编译运行。
- [ ] 性能以可测描述验收（可能与后续需求冲突，届时更新）：例如“千万级点云
  加载后，连续拖拽 ROI 框 5 秒，松开 3 秒内恢复响应”；显示降采样上限 150 万点。
- [ ] 批量验收（目标值可随本机实测校准，见 §8.6）：100 帧 × 3 盒（155 万点/帧）
  逐帧流式全流程 < 60s、峰值内存 < 2GB；全量 / 限量 / 逐帧三种模式帧序与保存
  命名一致；视口显示数据 ≤ 3000 万点（超限提示而非崩溃）。
- [ ] 中英双语 + 明暗主题完整覆盖；GUI 全流程（加载 → 筛选 / ROI → 分割 / 聚类
  → 保存）无需编程即可完成。

## 7. 技术决策记录（ADR，永久保留，只增不改，不删除历史）
| 日期 | 决策 | 备选方案 | 理由 |
|---|---|---|---|
| 2026-08-18 | 自研 QGraphicsView 画布，不迁移 QtNodes | QtNodes v3.0.9 | 渐进增强，布局已获用户认可 |
| 2026-08-18 | `Node::executeAll` 支持多输出（box_roi → cloud + region） | 单输出 + 特殊节点 | 端口类型清晰、组合自由 |
| 2026-08-18 | ROI 一等类型 `RoiBox` 挂 `Object.roi`；`RoiSelector` 独立模块 | 各视窗自实现 | 复用、统一交互 |
| 2026-08-18 | 零依赖 mini JSON（pipeline 层 Qt-free） | nlohmann/json | 无头运行与轻量 |
| 2026-08-20 | 连线跟随 BUG：加 `ItemSendsGeometryChanges` 标志 | 手动刷新连线 | 根因修复 + QtTest 回归 |
| 2026-08-24 | `RoiBox` 升级 OBB（center + half + orientation）；vtkBoxWidget2 启用旋转 | 仅 AABB | 客户需要旋转框选 |
| 2026-08-24 | ROI 位姿从表示层 8 个角点读取，不用 GetTransform 分解 | GetTransform position | 原点居中放置时 GetPosition = R·c 会偏 |
| 2026-08-24 | 包围盒计算跳过 NaN / Inf（`filters::computeBounds`） | std::min/max 全量 | RVC 点云空洞会污染结果 |
| 2026-08-24 | `ParamDef.default_value` 按类型规整 | 仅写在 description | 修复默认值 0 与 File 类型闪退 |
| 2026-08-24 | 显示降采样 ≤150 万点 + 单精度顶点 | 全量上传 GPU | 核显 / 远程桌面卡顿崩溃 |
| 2026-08-24 | QThread 取消机制（节点间 atomic cancel） | 直接销毁线程 | 运行中销毁 = Qt6 fail-fast |
| 2026-08-24 | 保存节点“文件夹 + 文件名”，auto → PLY，自动建目录 | 单路径输入框 | 用户语义明确，防“文件写到别处” |
| 2026-08-25 | 采用新项目管理文档模板 + git 本地提交照常 | 旧滚动式 STATUS | 交接块续接、防膨胀、可回溯 |
| 2026-08-25 | 节点 I/O / 批量 / 分块 / 显示统一约定（PROJECT §8）：ObjectList 载体、cloud/region/any 端口、zip 批量、分块执行 K=1/10/N、一帧多盒、视窗多图层显示 | 各节点自定语义 | 后续全部节点开发的统一约束，先行约定再实现 |

## 8. 节点 I/O、批量执行与显示统一约定（2026-08-25 用户逐项确认）

> 本节是后续所有节点开发的统一约束；与本节冲突的旧实现按“代码以约定为准”原则
> 逐步修正，待修清单见 §9。涉及需求 / 边界变化时先更新本节再实现。

### 8.1 数据结构契约
- 所有端口（输入 / 输出）只传输 `core::ObjectList`（有序 `PointCloudObject` 列表）；
  列表顺序即信息，输出顺序必须与输入顺序一致且可预测。
- cloud 端口对象：`cloud` 非空；`source_indices` 非空且长度 = 点数（红线落点）。
- region 端口对象：`cloud` 可为空；携带 `roi` 或 `regions+params`（几何描述）；
  **不携带点索引**——点索引只存在于对应 cloud 输出对象的 `regions`，避免无宿主二义性。
- 对象 id：派生对象 = `父id + "." + 后缀`（如 `frame_000.roi0`）；同次运行内唯一即可。
- `RoiBox` 增加 `label` 字段（如 "roi0" / "roi1"），用于一帧多盒的区分与显示。

### 8.2 端口类型
| kind | 输入要求 | 输出保证 | 适用 |
|---|---|---|---|
| `cloud` | 每对象有有效点云 | 每对象有点云 | 过滤 / 分割 / 聚类 / ROI / IO |
| `region` | 几何 / 区域描述，可无点云 | 同左 | box_roi 输出；roi_crop / 拟合 / 测量消费 |
| `any` | 不关心内容 | 不保证内容 | display3d 等通用端口（save 写点云，保持 cloud） |
- 兼容规则：`any` 通配；同类互通；异类禁止连线（错误前置到连线时）。
- 扩展规则：新增端口类型（image / pose / scalar 等）必须三件事一起做：
  ① 在 `PointCloudObject` 增加承载字段（不破坏 ObjectList 红线）；② 更新本表；
  ③ 在注册 / 类型名处登记。禁止隐式类型转换。

### 8.3 批量语义
1. **逐对象执行**：节点对输入列表的每个对象独立执行算法；并行是实现细节，输出顺序必须稳定。
2. **多输入对齐（zip + 广播）**：已提供数据的输入列表长度必须相同或为 1（1 广播到其它长度）；
   长度不同且无 1 → `std::runtime_error` 进 `lastError`。第 i 个处理单元 = 各端口第 i 个对象。
3. **输出形态**：
   - 1:1（过滤、ROI 裁剪、保存透传）：输出对象数 = 输入对象数，第 i 个对应第 i 个输入。
   - 1:N（平面检测、聚类、多盒 box_roi）：每输入对象产出 kᵢ 个对象，按输入顺序展平（帧主序）。
   - 多输出端口：同一处理单元的结果在各端口**同一次序**放置。
     box_roi：输入第 i 帧、盒列表 M 个盒 → cloud 输出与 region 输出各 F×M 个，
     下标 j = i*M + k，`cloud[j] ↔ region[j]` 严格对应，且 `cloud[j].roi == region[j].roi`。
   - 每帧不同盒（盒数 / 位置逐帧不同）不在本次实现范围，后续通过 region 输入端口
     按索引对齐扩展（2026-08-25 讨论结论）。
   - 使用流程：批量执行前用户先加载单帧设置盒列表（节点级参数），批量时同一套盒
     作用于所有帧。
4. **空输入与 optional**：输入端口声明是否 optional。optional 端口空 / 未连 → 节点按文档化
   默认行为执行（roi_crop 空 region = 透传）；非 optional 端口空列表 → 空传播（输出空，不报错）。
   多输入时若某输入为空而其它输入长度 >1，不适用空传播，按 8.3.2 报长度不一致。
   源节点（load / camera）例外：参数缺失才报错。
5. **错误处理**：批量中任一对象失败 = 整节点失败（不做部分成功）；`std::runtime_error` →
   `lastError`，错误信息带对象 / 帧标识（如“第 37 帧失败”）。

### 8.4 分块执行（性能模式）
- 统一“分块执行”，块大小 K 一个参数覆盖三种模式：**K=1 逐帧流式（默认）、
  K=10 限量读取（默认 10，可设）、K=N 一次性全读**。单文件恒为 K=1。
- 分块是**执行引擎层能力**：每块执行一次 Graph，块间清理中间结果缓存（只保留显示所需）；
  节点接口不变，节点不得假设能看到整批数据。
- 源节点按窗口读取（引擎注入 batch_start / count 瞬态上下文，节点保持无状态）。
- 文件夹按文件名排序确定帧序；三种模式帧序一致 → 保存命名（零填充 `_000`）跨模式不变。
- 文件夹为空或无匹配格式 → 输出空列表（按空传播处理，不报错；UI 可给提示）。
- 失败策略：默认 fail-fast（错误带帧号）；“跳过继续 + 失败清单”留作后续可选项。
- 取消与进度：块边界可取消（复用节点间 atomic cancel 机制）；进度 = 已完成块数 / 总块数。
- 内存指引：峰值 ≈ 帧大小 × K × 活跃节点数（约 3~5 层）；按“预算 ≤ 可用内存 1/3”反推 K。

### 8.5 对象派生与溯源
- 派生对象继承父对象 `unit / source_path / frame_id / visible / display_color`；
  `source_indices` 必须重映射（`composeIndices`）。
- `source_indices` 的索引语义：批量读取多帧时每帧是独立文件，`source_indices` 为
  **该帧文件内行号**；帧标识由 `frame_id` / `source_path` 承担，不引入跨文件全局
  点索引（2026-08-25 已确认）。
- `provenance` 统一为**当前执行节点 id**（旧实现保留父节点 id 的 3 处待修，见 §9）。
- 批量保存 = 每对象一个文件，命名 = 对象 name + 零填充序号（多盒时含盒 label）。

### 8.6 性能与内存约定
- 基准场景：单帧 155 万点（真实 RVC 数据）、500 万点合成帧、100 帧文件夹批量；
  性能用例阈值 ±20% 告警（不硬失败），本机实测后校准。
- 目标值：单帧加载 < 1s（含单位换算）；单帧单盒裁剪 < 50ms；100 帧 × 3 盒全流程
  < 60s 且峰值内存 < 2GB（流式）；UI 交互松手 3s 内恢复（§6 既有验收句保留）。
- 显示目标：流式模式新帧数据就绪 → 画面可见 < 100ms；批量执行中 UI 不逐帧卡顿
  （latest-wins 合并渲染）；模板图层不随帧重建（同一 actor 复用，可测）。
- 优化流程：先用性能分析器定位热点（Tracy / VS Profiler）再优化；并行优化前先跑
  单线程基准，避免内存翻倍。
- 数据布局：点云行访问密集，可评估 `Eigen::MatrixXf` 换行主序（允许但不强制）。
- 多盒裁剪实现要求：先对盒列表做并集 AABB 粗筛（O(N)），只对命中点逐盒精判，
  并复用临时缓冲，避免 M×N 次全扫描（支撑单盒 < 50ms 目标）。
- 显示降采样：单对象 ≤ 150 万点 + 单精度顶点（既有红线不变）。
- **视口上限预算 3000 万点**（依据：相机最大分辨率约 500 万点 × 6 帧同显预留）；
  上限管“容量”，超限提示而不是静默丢数据 / 崩溃。
- 实际渲染密度按硬件档位自适应：核显 / 远程桌面默认低密度（≤ 150 万 ~ 200 万活动点），
  强机可接近全预算；LOD（交互低密度、静止高密度）作为后续优化方向。

### 8.7 视窗显示模型
- 一个视窗 = **有序图层列表**；一个图层 = 一个 display3d 节点（同一视窗允许多个
  display3d 叠加，先创建的为底层）。模板匹配 = 模板图层（只执行一次，常驻）+
  当前帧图层（逐块更新）。
- 更新方式：**latest-wins 合并渲染**（处理线程只放最新帧到待渲染缓冲，UI 线程定时器
  合并渲染，丢弃过期帧）；**图层级增量更新**（只更新变化图层的点数据 / 演员，其它图层零重建）。
- 批量显示规则：逐帧 = 显示当前帧；分块 = 显示本块最后一帧；一次性 = 显示全批最后一帧；
  多帧同窗叠加由多个 display3d 图层完成，display3d 自身不累积状态。
- ROI 叠加只属于交互层（当前编辑帧），不叠加到模板 / 历史帧。
- display3d 输入端口类型为 **any**，为后续叠加 region 几何（平面 / 圆等）预留接口。

### 8.8 测试要求
- 每个节点 E2E 单测覆盖：单对象、多对象批量（≥2）、1:N 展平顺序、多盒对应、
  空列表、多输入对齐 / 广播 / 长度错误、`source_indices` 溯源、分块 / 流式帧序。
- 多输入对齐抽公共助手（`node_utils::alignInputs`）复用，禁止各节点自写。

## 9. 按约定待修清单（实现阶段，逐项提交）
- [x] box_roi region 输出坍缩为 1 个对象 → 按 (帧, 盒) 输出 F×M 个，与 cloud 端口对齐
- [x] roi_crop 只取 region 列表第 0 个 → 按索引对齐取第 i 个（zip + 广播）
- [x] `transformObject / makeSubsetObject / cropObject` 的 `provenance` 保留父节点 → 改为当前节点 id
- [x] `RoiBox` 增加 `label` 字段；box_roi 支持一帧多盒（box_count + boxes_json，
      盒列表节点级共享；多盒交互编辑 UI 为后续工作）
- [x] load_cloud 支持文件夹批量 + 读取模式（all / chunked / stream，默认逐帧）；
      批量读取优先二进制格式（PLY binary / PCD），并按文件名主干填充 frame_id
- [x] save_cloud 零填充命名与多盒命名（`frame_000.roi0.ply`），三模式命名一致
- [x] 抽 `alignInputs` 助手 + 批量 E2E 单测（§8.8 核心链路已覆盖；
      其余节点批量覆盖在下一步逐节点补齐）
- [ ] 输入端口声明 optional 标志（Node API），供 roi_crop 等 optional 端口按文档化默认行为执行
- [x] display3d 输入端口类型改 any；多 display3d 图层叠加；latest-wins 合并渲染
      （块快照 + 100ms 合并定时器）；视口 3000 万点预算与硬件档位
      （LOD 密度自适应留待后续）

## 10. 变更记录
| 日期 | 变更内容 | 影响范围 | 修改人 / 会话 |
|---|---|---|---|
| 2026-08-25 | 按新模板重写 AGENTS / PROJECT / PLAN / STATUS；新增 docs/SESSION_PROMPTS.md；git 初始化基线 | 全部文档 | Codex 会话 |
| 2026-08-25 | 新增 §8 节点 I/O / 批量 / 分块 / 显示统一约定与 §9 待修清单；§2 / §6 / §7 同步补充 | PROJECT / STATUS / PLAN | Codex 会话 |
| 2026-08-25 | §9 前 7 项批量架构落地（load 批量 + 分块执行、display3d 多图层 + latest-wins + 视口预算 + 硬件档位、一帧多盒 + provenance、save 零填充 / 多盒命名、alignInputs + 批量 E2E） | PROJECT / STATUS / PLAN | Codex 会话 |
| 2026-08-26 | 属性面板改造为选中节点的输入/输出逐帧树（多选驱动 3D）；ROI 基准按选中输入帧；box_roi 新增 frame_filter（未选中帧 1:1 透传、region 行带无效盒） | PROJECT / STATUS / PLAN | Codex 会话 |
