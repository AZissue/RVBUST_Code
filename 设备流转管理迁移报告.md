# 设备流转管理迁移报告

日期：2026-09-17  
开发分支：`feature/device-flow-html-full-21b4129`  
基线：`crm_system_v2` 的 `21b4129`  
状态：本地隔离预览可用，尚未上传 GitHub，尚未部署到正式数据库。

## 1. 来源与范围

- 来源文件：用户提供的《设备维修借测管理工具.html》。SHA-256：`324615f8b404549186f4dc6fb81a2484a36222149af28ae2e2596f726b9cc652`。
- 保留其 HTML/CSS/JavaScript 页面结构和主要交互；仓库中的 `apps/web/public/device-flow.html` 去掉了内嵌真实业务数据，数据从登录保护的 API 读取。
- 源文件内嵌数据有 1,102 条借测记录、0 条维修记录、75 条设备档案、37 条跟进、0 份图片和 0 份协议。源文件之外、只存在于原浏览器 localStorage 的后续数据无法从该 HTML 文件恢复，需要另行导出后再迁移。

## 2. 文件变更

- 修改：`apps/api/prisma/schema.prisma`、`apps/api/src/app.module.ts`、`apps/web/src/App.tsx`、`apps/web/src/components/AppShell.tsx`。
- 新增：`apps/api/prisma/migrations/202609160001_html_device_flow/`、`apps/api/src/html-device-flow/`、`apps/web/public/device-flow.html`、`apps/web/public/flow-online.js`、`apps/web/src/pages/DeviceFlowHost.tsx`、`migration/import-html-flow.mjs`、`e2e/html-device-flow-preview.spec.ts`。
- 原有客户、工单、借测、维修、设备、用户、权限、附件等业务模块的表和 API 实现未改。旧设备详情路由保留给其他模块已有链接，设备管理列表入口已指向新设备流转页面。

## 3. 数据库和 API

- 旧表：没有 `ALTER`、`DROP`、清空或字段含义变更。新增的数据库迁移只创建 `html_flow_state`、`html_flow_followups`、`html_flow_files` 三张独立表；提供 `down.sql` 供人工确认后回滚，未在正式库执行。
- `html_flow_state` 用 JSONB 保存原 HTML 的记录、设备、回收站和设置，并用 revision 防止旧页面覆盖新版本；跟进历史单独存在 `html_flow_followups`；附件元数据存在 `html_flow_files`。这些表不覆盖旧 CRM 业务数据。
- 新增 `GET /api/html-device-flow/snapshot`、`POST /api/html-device-flow/sync`、`POST /api/html-device-flow/files`、`GET /api/html-device-flow/files/:id`。旧 API 的请求/响应未修改。新 API 沿用当前会话与角色保护。
- 图片和协议文件保存在 `UPLOAD_DIR/html-device-flow/`，业务记录只保存受登录保护的文件 URL。取消引用后会删除关联附件；数据库与磁盘应一并纳入备份。
- 多人顺序使用可持久化；若两个用户基于同一版本并发修改，后保存者收到冲突提示并被阻止覆盖，需要重新加载。这不是自动合并，同一时间频繁协作仍需进一步做记录级同步。

## 4. 页面与功能映射

| 原 HTML 功能 | 迁移位置 |
| --- | --- |
| 设备总览、统计、待办、设备档案 | `/device-flow` 内的原 HTML 页面 |
| 维修、借测列表/筛选/排序/详情/编辑 | 同一页面的维修管理、借测管理 |
| 搜索、智能识别、跟进时间线 | 原 HTML JavaScript，业务数据通过 `flow-online.js` 在线保存 |
| 图片上传/换位/Lightbox/打包下载 | 原 HTML 交互 + 新文件 API |
| 借测协议上传/查看/识别 | 原 HTML 交互 + 新文件 API |
| 返厂单 PDF、Excel 导出 | 原 HTML 生成逻辑 |
| 回收站、恢复、永久删除 | 原 HTML 交互 + 在线状态保存 |

设备管理导航只显示一个新入口，原列表路由重定向至新页面；旧设备、借测、维修的详情链接及底层数据仍保留，不再作为设备管理主界面展示。原 HTML 使用的 PDF、OCR、Excel 前端库仍来自公共 CDN；断网或 CDN 不可用时，相应导出/识别功能可能不可用，需在正式内网部署前本地化依赖。

## 5. 数据迁移与保护

- 仅在独立预览数据库 `127.0.0.1:5544/flow_github_21b4129` 执行新增迁移与导入；未触碰 `D:\RVBUST\tech-support-crm-v2` 的正式数据库、`.env`、加密密钥或上传目录。
- 导入器默认只读检查；只有带 `--apply-preview` 且目标数据库名称和端口完全匹配时才写入。预览库导入后再次核对：1,102 条借测、0 条维修、75 条设备、37 条跟进，业务数据无导入失败；预览测试记录已清理。
- 原 CRM 旧设备数据保留在原表，新页面目前不会自动把旧表记录合并为 HTML 设备档案。若需要在新界面同时展示旧 CRM 历史设备，应先确定去重和字段映射规则，再做单独迁移。
- 正式上线前需备份数据库及 `UPLOAD_DIR`，保留 `.env` 和 AI 加密密钥，并对原 HTML 文件之外的浏览器本地数据做独立核查。

## 6. 验证结果

- API/Web 生产构建通过；原有 API 单元测试 136 项通过，Web 单元测试 4 项通过。
- Chrome/Playwright 预览测试通过：登录、读取 1,102 条借测和 75 条设备、逾期筛选与排序、借测 Excel 下载、服务器保存与刷新持久化、维修新建和返厂单 PDF 下载、借测协议和图片上传/读取、中文文件名、移除记录后附件清理。390px 窄屏已检查，无页面级横向溢出。
- 预览链接：`http://127.0.0.1:5177/device-flow`。API：`http://127.0.0.1:3005/api/health`。仅供本机隔离预览。
- 尚未逐项自动化验证：扫描 PDF OCR 的准确率、所有智能识别边界样例、外部文件拖入、图片拖动换位、完整移动端交互，以及旧 CRM 全业务端到端流程。上线前仍需人工验收这些路径。

## 7. 当前限制与下一步

1. 原 HTML 之外的 localStorage 数据没有包含在用户提供的文件中，不能据此声称已经完整迁移所有浏览器本地记录。
2. 全模块业务状态目前集中在独立 JSONB 表，revision 保护防止覆盖，但并发编辑需要重新加载；长期多人高频使用应改为记录级 API 和自动冲突处理。
3. PDF/OCR/Excel 依赖公共 CDN，正式部署前建议打包为本地静态资源并验证 OCR 模型下载。
4. 正式数据库尚未执行迁移与导入，GitHub 尚未上传。请先在本地预览核对布局、真实记录和关键操作，再决定正式发布。

## 8. 交互调整（2026-09-17）

- 设备总览、维修管理、借测管理和回收站继续集中在设备流转页面；没有把这些操作拆回原 CRM 的不同页面。
- 左侧新增“返回技术支持系统”，可直接回到原工作台，避免从全屏设备流转页面迷失。
- “新建设备”从占满屏幕高度的侧边抽屉改为居中紧凑弹窗；设备完整历史仍使用侧边抽屉，以容纳较长的记录。
- 新设备弹窗支持取消、点击遮罩和 Esc 关闭；已验证桌面及 390px 窄屏位置，未改变业务数据结构。

## 9. 内嵌化改造（2026-09-17 第二轮）

- 设备流转不再是独立全屏页面 + iframe，而是技术支持系统内部路由 `/device-flow` 的一个普通页面：保留系统顶部栏与左侧主导航，页内用四个标签（设备总览 / 维修管理 / 借测管理 / 回收站）切换内容，不再出现第二套侧边栏。
- 页面结构拆分为：`apps/web/src/pages/device-flow/DeviceFlowPage.tsx`（React 挂载与脚本加载）、`device-flow-body.html`（页面片段）、`device-flow.css`（样式，变量映射到系统主题、支持暗色）、`apps/web/public/device-flow-app.js`（业务逻辑，由独立 HTML 内联脚本迁移而来）。`DeviceFlowHost.tsx` 与 `device-flow.html` 已删除。
- 新建/编辑维修单、借测单改为页内右侧抽屉（原 `newRecord` 整页表单）；新建设备仍为居中紧凑弹窗；查看设备/工单详情、设备完整历史仍为侧边抽屉。弹窗/抽屉均支持取消按钮、遮罩和 Esc；表单有内容时关闭前会确认，关闭后回到原列表并保留筛选与位置。
- 性能：业务数据增加模块级缓存，避免每次读取都对 1102 条记录 `structuredClone`（原切换卡顿主因）；标签页懒渲染，只有首次进入或数据变化后才重建对应列表，纯切换零重建；Excel/PDF/压缩/OCR 五个 CDN 组件改为按需加载，不再随页面启动拉取；附件仍只在打开详情时加载。
- 每个标签的滚动位置与当前标签记录在 sessionStorage，刷新后恢复；`?tab=` 参数仍然有效（旧 `/loans`、`/repairs` 重定向入口不变）。
- 顺带修复：智能识别“识别并填入”中物流字段引用不存在的输入框 id 导致脚本中断的问题（改为 `rInboundCarrier` / `rInboundTracking` / `rOutboundCarrier` / `rOutboundTracking`）。
- API 与数据库零改动，仍复用 `/api/html-device-flow/*`；未重新导入或覆盖真实数据。

## 10. 借测管理细节修复（2026-09-17 第三轮）

- 详情“设备六面图 / 配件图”空态新增“添加图片”入口，点击打开该工单的编辑抽屉并定位到 8 格图片区；有图片时操作栏显示“一键打包下载图片”（无图片时不显示）。图片仍上传至服务端文件存储（`/api/html-device-flow/files`）。
- 借测单措辞去维修化：表单与详情、内联编辑的备注标题改为“借测备注”（维修单保留“维修过程 / 备注”），借测备注占位提示不再出现维修过程/维修动作/更换件等字样；借测物流区重排为“寄出物流/寄出单号 → 归还物流/归还单号 → 状态 → 配件”。
- 跟进情况表单重排：日期（默认当天）、跟进人（可选可输）、跟进内容（必填，占位改名）与“添加跟进”按钮在抽屉内对齐不越界；窄屏单列。
- 右下角“已连接/已保存”常驻提示改为成功 2 秒自动消失；保存失败与多人编辑冲突仍为常驻红色提示（含“重新加载”按钮），在线保存逻辑不变。
- 修复 Lightbox 打开时按 Esc 会级联关闭详情抽屉的问题（现在 Esc 只关闭大图）。
- `saveRecord`/`ensureDeviceFromRecord`/`addFollowUp`/`removeFollowUp` 的保存改为 await，消除刷新瞬间未落库保存与新版面并发造成的 409 冲突窗口。
- e2e 增至 7 项并全部通过：新增“无图片借测单 → 添加图片 → 保存 → 刷新 → 预览大图 → ZIP 下载 → 添加跟进 → 刷新确认历史”的完整旅程断言，以及借测措辞与物流顺序断言。

## 11. 设备服务管理整改（2026-09-17 第四轮）

- 页面与侧栏更名“设备服务管理”；业务命名统一为“维修工单 / 借测单”，移除“返厂维修/返厂单”等旧称。
- 设备总览右上角“+ 新建设备”按钮与旧弹窗移除；设备档案仍由工单自动建立，可查看/删除/恢复，数据不删。
- 维修工单“关联设备 SN”改为选填：新建/编辑校验只要求客户；SN 留空不产生设备关联（列表显示 SN: -），选择已有 SN 仍自动带出型号/客户并保持关联，智能识别未识别到 SN 不阻塞建单。SN 自动带出客户由 blur 时机改为 input 时机，避免与输入过程竞争。
- 返厂单 PDF 整体替换为“维修工单 PDF”：新建/编辑抽屉、详情操作栏、内联编辑均有“生成维修 PDF”，生成前先把最新修改落库（新建自动转编辑态防重复建单）；内容来自实际保存数据（客户与联系人、型号与可选 SN、报修日期、故障现象、维修过程、处理结果、寄返信息），空字段显示“未填写”；文件名 `维修工单-单号-客户.pdf`；中文与多页沿用 html2canvas+jsPDF 渲染。保存不再自动下载 PDF。
- e2e 共 8 项全部通过，新增“无 SN 建单（不产生虚假设备）、已有 SN 建单（关联保持）、编辑后生成 PDF、刷新后数据仍在”断言；真实数据保持 1102 条记录 / 75 台设备。
