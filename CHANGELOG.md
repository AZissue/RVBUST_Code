# 更新日志

本文件记录 MultiCameraCalibration 每个版本的缺陷修复、功能新增与优化。

版本号规则（语义化版本）：
- `v1.0.x`：小修复 / 文案调整 / bugfix / 日志与文档更新
- `v1.x.0`：中等规模（功能优化、模块重构、新增子功能）
- `vx.0.0`：大型功能 / 架构升级（全新模块、重大重构、里程碑发布）

运行时版本号会在基础版本后自动附加 Git 短 hash，例如 `v1.0.4+g<hash>`。

---

## [1.1.1] - 2026-08-31

### Fixed
- **P2-c1** `ChainStitcher.get_merged_pointcloud()` 返回内部缓存的深拷贝，
  防止调用方修改后污染缓存。
- **P2-c2** 新增 `pcd_utils.merge_pointclouds()`，合并前统一 colors/normals 属性，
  修复 `o3d.PointCloud +=` 在属性不对称时颜色/法向量被清零的问题；
  已应用到 `chain_stitcher`、`stitch_engine`、`robot_stitch_workflow` 三处。
- **P2-c10** `TurntableCalibrator.generate_sequence()` 每帧先复制 base_pcd 再 transform，
  避免原地修改导致所有帧变成同一姿态；同步修复原型侧相同 bug。

---

## [1.1.0] - 2026-08-31

### Fixed
- **N2** 主窗口重新打开设备管理后连接设备时显示 loading 遮罩；
  若连接失败，弹窗提示并允许回退到之前的模式。
- **N4** 转台 360° 自动采集改为当前帧完成后再调度下一拍，
  避免采集慢于间隔时 worker 堆积、帧乱序。
- **P2-2** `mobile_chain_workflow.load_session_dir` 加载失败时回滚到加载前状态，
  不再留下半初始化的链式拼接器。

### Changed
- **N5** 离线拼接原型 `simple_ui.py` 拼接循环内增加 `QApplication.processEvents()`，
  loading 遮罩现在能正确显示实时进度。

---

## [1.0.9] - 2026-08-31

### Fixed
- **N1** 主窗口关闭时选择保存会话后同步等待保存完成，避免异步保存被中断导致文件半写。
- **N3** 修正模式 B/C 打开离线会话后的日志文案：
  不再无条件显示"会话加载完成"，模式 B 提示时间线恢复待实现，模式 C 提示暂不支持会话恢复。
- **P2-u2** 单相机卡片 3D 拍摄失败后恢复该卡片的 2D 预览勾选状态。
- **P2-u3** 实时取景面板 `set_frame(None)` 现在会清空画布，不再保留最后一帧。
- **P2-u8** 多相机外参标定模式要求至少 2 台相机成功连接才置 connected；
  转台 360° 拼接模式要求至少 1 台相机成功连接才置 connected。

---

## [1.0.8] - 2026-08-31

### Fixed
- **P0** 修复单相机移动拼接离线加载会话后删除/撤销/失败帧会删除整个会话目录的数据丢失问题：
  `load_session_dir` 中每帧 `offline_dir` 改为站位子目录，与在线拍摄路径一致。
- **P1** 修复 `load_session_dir` 按字符串排序导致 `station_10` 排在 `station_2` 前面的问题，
  改为按站位序号数字排序，避免 ≥10 机位会话丢站/参考系错位。
- **P1** 同步 `prototypes/turntable_360_stitch/core/turntable_calibrator.py` 的转台点云复制修复，
  避免原地 transform 导致多次拼接结果漂移。
- **P2** 修复 `src/core/turntable_calibrator.py` 拼接消息构造时未过滤 `None` 帧的潜在崩溃。
- 转台模式拼接成功后也置脏标记，关闭窗口时触发未保存确认。

### Changed
- 复选框选中态增加白色对勾 SVG，提升可访问性。

---

## [1.0.7] - 2026-08-28

### Changed
- 进一步优化多相机外参标定工作区同步/异步拍摄单选按钮：
  指示器改为正圆形，选中态显示品牌红圆环 + 白点，与复选框视觉明显区分。

---

## [1.0.6] - 2026-08-28

### Changed
- 多相机外参标定工作区「已连接设备」列表选中样式现代化：
  移除焦点虚线框，选中项改为左侧品牌红强调边 + 半透明选中底色。
- 同步/异步拍摄单选按钮样式现代化：自定义 16px 圆角指示器，
  选中态显示品牌红实心圆环，悬停/禁用态统一视觉层级。

---

## [1.0.5] - 2026-08-28

### Fixed
- 修复转台 360° 拼接模式进入时崩溃：
  `TurntableWorkspace.set_camera_manager()` 存在两个重复定义，
  导致 `main_window.set_mode()` 调用参数不匹配；
  移除旧的两步注入方法，统一使用合并后的 `set_camera_manager(cam_mgr, marker_detector)`。
- 修复转台模式连接相机遮罩卡住的问题，现在可正常进入主功能页面。

---

## [1.0.4] - 2026-08-28

### Changed
- 转台 360° 拼接工作区移除独立的「初始化 RVC / 刷新 / 连接相机 / 断开」按钮，
  改为直接使用 LauncherDialog 已连接的共享相机管理器。
- 转台 360° 拼接 2D 预览区改为带标题栏的卡片式面板，UI 更现代。

---

## [1.0.3] - 2026-08-28

### Changed
- 暂时移除主窗口顶部「后处理」入口按钮与 `_PostProcessDialog`；
  点云预览窗口为保证运行性能默认执行的显示级下采样仍然保留；
  后处理功能将在后续版本中通过独立设置/预览页面重新提供。
- 更新 README.md 与 docs/README.md，同步模式说明、开发计划与 CHANGELOG 索引。

---

## [1.0.2] - 2026-08-28

### Fixed
- 单相机移动拼接模式 B 进入时自动创建空会话目录的问题：现在会话目录延迟到首次拍摄机位时才创建。
- 关闭主窗口时无数据也会提示保存的问题：现在仅当存在未保存的拍摄数据时才弹窗，并提供「保存 / 不保存 / 取消」三选一。

### Added
- 单相机移动拼接工作区新增「离线加载会话」按钮，可选择已保存的 `session_*` 文件夹，自动识别 `station_N/station_N.png + station_N.ply` 并按顺序拼接。

---

## [1.0.1] - 2026-08-27

### Changed
- 版本号移除 `alpha` 后缀，进入正式语义化版本管理。
- `version.get_version()` 运行时自动附加当前 Git commit 短 hash。
- 程序启动时在日志中输出当前版本号，便于排查问题。
- 新增 `CHANGELOG.md`，集中记录版本变更与缺陷修复。

---

## [1.0.0] - 2026-08-27

首个正式版本，包含以下主要功能与缺陷修复：

### Added
- **多相机外参标定模式**：支持多相机同步/异步拍摄、编码圆检测、外参计算与保存。
- **单相机链式拼接模式**：单相机移动拍摄，多帧按编码圆配准并拼接。
- **转台 360° 拼接模式**：作为第三种独立模式并入主程序，支持转台场景点云拼接。
- **编码圆标定板生成器**：
  - 支持扇区数 N、同心圆半径比、整体缩放等参数实时调节；
  - 预览图与导出 PDF/PNG 严格一致；
  - 支持 A4 页面、打印 DPI、页面边距设置。
- **离线拼接测试工具（ui_v2 重构版）**：
  - 递归/模糊匹配本地图像与点云文件对；
  - 离线识别编码圆并支持加载 `coded_circle_meta.json`；
  - 拼接结果缓存，文件列表顶部新增【合并结果】入口，切换后不丢失；
  - 合并点云保存与显示分离，保存仍写完整分辨率。
- **3D 点云查看器**：
  - N 路相机点云叠加显示；
  - 合并结果独立显示模式；
  - 最大化、折叠、视角预设、坐标轴、网格、背景切换等工具栏；
  - 大数据量自动降采样渲染，避免 UI 卡死。

### Fixed
- **严重 bug**：从启动小窗切换模式后，主界面固定显示「多相机外参标定」且错误显示仅 1 台相机的问题。
- 自动配置 IP 时未显示 loading、UI 卡住的问题。
- 3D 查看器最大化/全屏按钮在多模式与离线工具中失效的问题。
- 转台模式进入后先跳到多相机拼接再闪回正确 UI 的问题。
- 主程序启动小窗完成连接后仍闪一下的问题。
- 主功能初始窗口超出屏幕分辨率、控件显示不全的问题。
- 编码圆生成器保存 PDF 时因源文件被占用导致失败的错误提示不清晰的问题。
- `BackendBridge` 中缺失 `Signal` 导入导致的启动报错。
- 离线拼接读取数据文件对失败、合并结果未缓存导致切换后消失的问题。
- 离线拼接 2600 万点合并结果渲染极慢的问题（显示级下采样 + 快速粗降路径）。

### Optimized
- 启动流程改为先渲染目标工作区 + loading，再后台连接相机，避免空白页面与闪烁。
- 主程序窗口启动时默认屏幕居中。
- 文本/标题边框统一去掉，蓝色框标题更醒目。
- 工具栏图标与按钮边缘清晰度优化。
- 日志窗口折叠按钮与图标样式优化。

---

## 历史预发布版本

### [1.0.0-alpha] - 早期验证阶段
- P1 算法验证版本，双模式工作流架构原型。
- 包含多相机标定与单相机链式拼接基础能力。
