# 技术支持 CRM 整体架构方案（定稿）

> 版本：v1.1（新增用户体系设计）
> 基于：`crm_system_v2` 现有代码（NestJS + React + Prisma + PostgreSQL）演进，不重写
> 目标平台：树莓派内网服务器 + 外网访问（Tailscale），GitHub 托管源码与镜像

## 一、决策记录

| # | 问题 | 决策 |
|---|---|---|
| D1 | 并发量 | 约 10 人同时在线 → 模块化单体 + PostgreSQL，不引缓存/队列 |
| D2 | 外网访问 | 需要 → Tailscale 虚拟组网为主方案（frp + 域名为备选） |
| D3 | 旧版数据 | 暂不迁入，**保留迁移入口**（改造现有 `migration/import-v1.ts`） |
| D4 | 客户 360 内容维护 | 内部自行维护 → 背景介绍/应用场景/项目需求用 Markdown 编辑器 |
| D5 | AI 网关 | 保留现状，本次不动代码；不配 Key 时自动降级，零运行负担 |
| D6 | 用户体系 | 自助注册 + **管理员审批**；两级角色：管理员 / 普通用户（即工程师）；工单、借测、返修均关联工程师用户 |

数据库选型结论：**保留 PostgreSQL 17**（10 并发对它毫无压力，Pi 4B/5 4GB 内存足够，且 Prisma 迁移链零改动）。SQLite 方案放弃。

## 二、总体架构

```
┌──────────┐   Tailscale 加密隧道    ┌─────────────────────────────────────┐
│ 外网用户  │ ◄────────────────────► │ 树莓派 (Pi OS 64bit, Docker Compose) │
│ (手机/电脑)│                        │  ┌─ Nginx (web:80)                  │
└──────────┘                        │  │    静态资源 + /api 反代             │
┌──────────┐                        │  ├─ API (NestJS, Node 20)           │
│ 内网用户  │ ── http://pi-ip ─────► │  │    模块化单体, 15+3 个业务模块      │
└──────────┘                        │  ├─ PostgreSQL 17 (named volume)    │
                                    │  ├─ uploads/ (附件, 数据盘)          │
                                    │  └─ cron: 每日 pg_dump 备份          │
                                    └─────────────────────────────────────┘
                                              ▲ 源码/镜像
┌─────────────────────────────────────────────┴──────────┐
│ GitHub (AZissue/RVBUST_Code, crm_system_v2 分支)         │
│  Actions: test → build → push 镜像到 GHCR                │
└──────────────────────────────────────────────────────────┘
```

- **GitHub 只托管源码和 CI 产物**，不运行服务；Pi 上 `docker compose pull && up -d` 完成更新
- 镜像走 GHCR，避免 Pi 直连 Docker Hub 被墙的问题
- 全系统运行时内存约 500~700MB（PG 300 + Node 200 + Nginx 少量）

## 三、用户体系设计 ★（本期新增）

### 3.1 角色模型（简化为两级）

| 角色 | 标识 | 说明 |
|---|---|---|
| 管理员 | `ADMIN` | 审批注册、管理全部用户、所有业务数据可见可管 |
| 普通用户 | `USER` | 即工程师：建工单、被指派工单/借测/返修、写工作记录、看客户 |

> 现有代码中的 `admin/support/employee/customer` 四角色收敛为 `ADMIN/USER`（support/employee 合并为 USER；customer 门户属非目标，角色枚举保留但无 UI）。权限判断从角色名字符串逐步收敛到这两个常量。

### 3.2 User 实体扩展

```prisma
enum UserStatus { PENDING 待审批 / ACTIVE 正常 / DISABLED 已禁用 }

model User {
  id            String     @id @default(uuid())
  username      String     @unique
  passwordHash  String
  name          String                    // 真实姓名（注册时必填）
  role          String     @default("USER")   // ADMIN | USER
  status        UserStatus @default(PENDING)
  department    String?                   // 部门（注册时选填）
  phone         String?
  email         String?
  // 关联：该用户作为工程师跟进的单据
  assignedTickets  Ticket[]      @relation("TicketAssignee")
  assignedLoans    LoanOrder[]   @relation("LoanAssignee")
  assignedRepairs  RepairOrder[] @relation("RepairAssignee")
  createdAt / updatedAt
  @@map("users")
}
```

### 3.3 注册与审批流程

```
用户提交注册（用户名/密码/姓名/部门/手机）
    → POST /auth/register（公开接口，限流，写注册审计）
    → 创建 status=PENDING 的用户，不可登录
    → 通知管理员（站内通知："新用户 xxx 待审批"）
    → 管理员后台审批：
        通过 → status=ACTIVE（可调整角色/部门）→ 用户可登录
        拒绝 → status=DISABLED（保留记录，审计留痕）
```

- 登录校验：PENDING 提示"等待管理员审批"，DISABLED 提示"账号已被禁用"
- 首个管理员：seed 创建（`SEED_ADMIN_PASSWORD` 机制保留）

### 3.4 管理员的用户管理后台

| 功能 | 说明 |
|---|---|
| 用户列表 | 全部注册用户，按 状态/角色/部门 筛选，含注册时间、最近登录 |
| 待审批 Tab | 突出显示 PENDING 用户，一键通过/拒绝 |
| 编辑 | 改姓名/部门/角色/手机/邮箱 |
| 禁用/启用 | DISABLED 立即吊销全部会话（修复审查报告问题：改状态不杀会话） |
| 重置密码 | 生成临时密码 or 管理员设新密码，同时吊销全部会话 |

### 3.5 工程师关联设计

三类业务单都关联**工程师用户**（assigneeId），语义统一为"跟进工程师"：

| 单据 | 字段 | 说明 |
|---|---|---|
| Ticket | `assigneeId`（已有） | 负责人工程师 |
| LoanOrder | `assigneeId` 新增 | 借测跟进工程师（追还、协调归还） |
| RepairOrder | `assigneeId` 新增 | 返修跟进工程师（对接客户、跟进维修进度） |

- 指派时发通知给工程师（接通 notifications 模块）
- 工程师登录后默认看板按 `assigneeId = 我` 过滤"我的工单/我的借测/我的返修"
- 客户侧对接人**不混用**用户表：仍是 customers 模块下的 `Contact`（客户公司的人），与内部工程师明确区分
- 权限：普通用户只能看/改**指派给自己**或**自己创建**的单据；管理员全量（复用 AccessPolicyService 模式）

## 四、数据库设计（PostgreSQL / Prisma）

在现有模型基础上**新增 3 个实体 + 扩展 User/Customer/Ticket**。风格与现有代码一致：UUID 主键、snake_case `@@map`、时间戳。

### 4.1 Device（设备）★ 新增核心实体

```prisma
enum DeviceStatus { IN_STOCK 在库 / LOANED 借出 / REPAIRING 返修中 / RETIRED 报废 }

model Device {
  id            String       @id @default(uuid())
  model         String                    // 产品型号 (M2600/X 等)
  sn            String?      @unique      // 出厂 SN，返修主键
  name          String?                   // 客户侧自定义命名
  customerId    String?                   // 当前归属客户（空 = 公司库存机）
  status        DeviceStatus @default(IN_STOCK)
  purchaseDate  DateTime?
  warrantyUntil DateTime?                 // 保修到期日，返修判保内/保外
  note          String?
  customer      Customer?    @relation(...)
  loanItems     LoanItem[]
  repairs       RepairOrder[]
  tickets       Ticket[]
  createdAt / updatedAt
  @@map("devices")
}
```

### 4.2 LoanOrder（借测单）+ LoanItem 新增

```prisma
enum LoanStatus { ONGOING 借出中 / OVERDUE 逾期 / RETURNED 已归还 / CANCELLED 已取消 }

model LoanOrder {
  id          String     @id @default(uuid())
  loanNo      String     @unique          // L-YYMMDDNNN
  customerId  String
  contactId   String?                     // 客户侧对接联系人
  assigneeId  String?                     // ★ 跟进工程师（关联 User）
  purpose     String                      // 借测目的/应用场景
  status      LoanStatus @default(ONGOING)
  loanedAt    DateTime
  dueAt       DateTime                    // 预计归还（逾期判断依据）
  returnedAt  DateTime?
  agreementNo String?                     // 借测协议编号
  note        String?
  createdById String
  createdAt / updatedAt
  items       LoanItem[]
  assignee    User?      @relation("LoanAssignee", ...)
  @@map("loan_orders")
}

model LoanItem {
  id            String     @id @default(uuid())
  loanOrderId   String
  deviceId      String
  returnedAt    DateTime?
  conditionNote String?                   // 归还时设备状态备注
  loanOrder / device @relation(...)
  @@map("loan_items")
}
```

### 4.3 RepairOrder（返修单）+ RepairEvent 新增

```prisma
enum RepairStatus { RECEIVED 已收货 / DIAGNOSING 检测中 / REPAIRING 维修中 / SHIPPED 已寄回 / CLOSED 已关闭 }

model RepairOrder {
  id          String       @id @default(uuid())
  repairNo    String       @unique        // RP-YYMMDDNNN
  deviceId    String                     // → SN/型号自动带出
  customerId  String                     // 冗余，便于按客户聚合
  contactId   String?                    // 客户侧对接人
  assigneeId  String?                    // ★ 跟进工程师（关联 User）
  symptom     String                     // 故障现象（客户描述）
  faultCause  String?                    // 故障原因（检测后填）
  status      RepairStatus @default(RECEIVED)
  inWarranty  Boolean?                   // 保内/保外（可按 warrantyUntil 自动预填）
  receivedAt  DateTime
  shippedAt / closedAt DateTime?
  trackingNo  String?                    // 寄回物流单号
  resolution  String?                    // 处理结果：换机/修复/退件
  note        String?
  createdById String
  createdAt / updatedAt
  events      RepairEvent[]
  assignee    User?      @relation("RepairAssignee", ...)
  @@map("repair_orders")
}

model RepairEvent {                       // 仿现有 TicketEvent 模式
  id, repairOrderId, authorId, type, content, metadata Json?, createdAt
  @@map("repair_events")
}
```

- 外观图片：复用现有 `files` 模块与 `Attachment` 表，新增 `repairOrderId` 外键，不建独立表。

### 4.4 Customer 扩展（客户 360）

| 字段 | 类型 | 用途 |
|---|---|---|
| `websiteUrl` | String? | 官网跳转 |
| `wikiRef` | String? | 百科词条链接（无官网时替代） |
| `background` | String? (长文本/Markdown) | 背景介绍 |
| `applicationScenarios` | String? | 使用我们产品的应用场景 |
| `projectNeeds` | String? | 项目需求描述 |

### 4.5 Ticket 扩展

- `deviceId String?` + 外键（工单可关联具体设备）
- 编号规则对齐旧版生产验证格式 `RVC-YYMMDDNNN`（类型前缀位已预留 VDA）
- 分类字典种子沿用旧版 16 类（有 441 工单的真实分布验证）：需求沟通/样品测试/SDK开发/手眼标定/技术咨询/软件问题/点云调试/资料提供/远程调试/硬件故障/点云质量/返修/售前评估/相机选型/项目评估/内部支持

### 4.6 实体关系总览

```
User(工程师) 1─n assignedTickets / assignedLoans / assignedRepairs
Customer 1─n Contact / Device / Project / Ticket / LoanOrder / RepairOrder
Device   1─n LoanItem / RepairOrder / Ticket
LoanOrder 1─n LoanItem         Ticket 1─n TicketEvent / Attachment
RepairOrder 1─n RepairEvent / Attachment
User n─1 各业务单 (createdBy)   —— createdBy 与 assignee 分离
```

## 五、后端模块设计

现有模块全部保留，新增 3 个 + 改造 2 个，保持 `module + controller + service + dto` 三件套：

```
apps/api/src/
├── auth/           改造：+ /register（公开、限流）+ 登录校验 status
├── users/          改造：审批/禁用/重置密码/吊销会话 + 列表筛选
├── devices/        新增：台账 CRUD + 状态机
├── loans/          新增：借测单 CRUD + 归还 + 指派 + 逾期扫描
├── repairs/        新增：返修单 CRUD + 状态流转（事务内写 RepairEvent）
├── customers/      扩展：profile 字段 + GET /customers/:id/profile 聚合
├── tickets/        扩展：deviceId + 编号规则
├── files/          扩展：attachment 支持 repairOrderId
├── notifications/  扩展：接通 注册待审批/工单指派/借测逾期/返修状态变更 四类事件
└── ...其余保留（含 ai/，按 D5 不动）
```

核心 API（全部走现有 AuthGuard + RolesGuard + AccessPolicy）：

| 方法 | 路由 | 权限 | 说明 |
|---|---|---|---|
| POST | `/auth/register` | 公开(限流) | 注册 → PENDING + 通知管理员 |
| GET | `/users?status=PENDING` | admin | 用户列表筛选 |
| POST | `/users/:id/approve` `/users/:id/reject` | admin | 审批 |
| POST | `/users/:id/disable` `/users/:id/enable` | admin | 禁用并吊销会话 |
| POST | `/users/:id/reset-password` | admin | 重置密码并吊销会话 |
| GET | `/customers/:id/profile` | 登录 | 360 聚合：profile + 工单 + 设备 + 借测 + 返修 |
| POST/PATCH | `/devices` `/devices/:id/status` | support+ | 设备台账 / 状态机 |
| GET | `/loans?status=OVERDUE` | 登录 | 借测台账（我的/全部按角色） |
| POST | `/loans/:id/assign` | admin | 指派跟进工程师 + 通知 |
| POST | `/loans/:id/return` | support+ | 归还：事务内关单 + 设备回 IN_STOCK |
| POST | `/repairs` | support+ | 创建：SN 查重、预填保内/保外 |
| POST | `/repairs/:id/transition` | support+ | 状态流转：事务内写 RepairEvent |
| POST | `/repairs/:id/assign` | admin | 指派跟进工程师 + 通知 |

### 借测逾期扫描

`@nestjs/schedule` 每日 cron：扫 `dueAt < now AND status = ONGOING` → 置 OVERDUE + 通知创建人和跟进工程师。**不引入独立调度器**。

## 六、前端 UI 设计

技术栈不变（React + TS + Vite + 现有 useRemote/SimpleFormModal/Status 组件模式），不引入新 UI 库。

### 6.1 信息架构

```
登录页 ──► 注册页（新）
工作台（仪表盘：我的工单/借测逾期/返修中卡片）
工单管理 ── 列表 ── 详情（事件流）
客户管理 ── 列表 ── 客户 360 详情页 ★
设备台账 ★
借测管理 ★
返修管理 ★
工作记录 / 工作事项（保留）
报表统计
系统设置 ── 用户管理（待审批 Tab）★ / 分类字典 / AI 设置
```

### 6.2 注册页 + 登录调整

- 登录页加"注册"入口 → 注册表单（用户名/密码/确认密码/姓名/部门/手机）
- 注册成功提示"等待管理员审批，通过后可用账号登录"
- 登录接口对 PENDING/DISABLED 返回明确提示文案

### 6.3 管理员用户管理页（改造现有 UsersPage）

- 顶部 Tab：全部 / 待审批（角标数量）/ 已禁用
- 行操作：审批通过 / 拒绝 / 编辑 / 禁用 / 启用 / 重置密码
- 角色改为下拉（ADMIN/USER），不再是手输字符串（修复审查问题）

### 6.4 客户 360 详情页

```
┌────────────────────────────────────────────────┐
│ ← 客户名称 [等级]                    [编辑资料] │
│ 行业 | 官网 ↗ | 百科词条 ↗                       │
├────────────────────────────────────────────────┤
│ 背景介绍  (Markdown 渲染，空则占位"待补充")       │
├────────────────────────────────────────────────┤
│ 应用场景 | 项目需求描述                          │
├─────────┬─────────┬──────────┬─────────────────┤
│ 联系人   │ 设备清单 │ 进行中工单 │ 借测/返修概要    │
├─────────┴─────────┴──────────┴─────────────────┤
│ [工单记录] [借测记录] [返修记录] [设备记录] Tab 列表│
└────────────────────────────────────────────────┘
```

### 6.5 借测 / 返修页

- 借测台账列：单号 | 客户 | 设备(SN) | 跟进工程师 | 借出日期 | 预计归还 | 状态（OVERDUE 红标）| 操作（归还/延期/指派）
- 返修台账列：单号 | SN | 型号 | 客户 | 故障现象 | 状态 | 跟进工程师 | 收货日期
- 返修详情：状态时间线（复用 TicketEvent 样式）+ 外观图片墙 + 故障原因/处理结果/物流单号编辑
- 两类单据的新建/编辑表单都有**跟进工程师**下拉（从用户列表拉 ACTIVE 用户）

## 七、部署架构（树莓派 + 外网）

```
                    ┌────────── Tailscale 隧道（WireGuard 加密）──────────┐
                    ▼                                                      ▼
外网用户(手机/电脑) ──► tailscale0 接口 ──► Nginx:80 ──► API:3001 ──► PostgreSQL:5432
                    │                     └────► 静态资源 (web/dist)
内网用户 ──► eth0/wlan0:8080 ────────────┘
```

- **Tailscale（主方案）**：Pi 装 tailscale，外网设备加入同一 tailnet 后直接用 MagicIP/机器名访问，零端口暴露、传输层已加密、自带 ACL。管理成本最低。
- **frp + 域名 + HTTPS（备选）**：有公网服务器时走这条，Nginx 层配证书。
- 备份：`pg_dump` 每日 cron → 数据盘 + 可选 rsync 到办公机；uploads/ 目录同步归档。
- 更新流：`git push` → Actions 跑测试+构建 → 推 GHCR → Pi `docker compose pull && up -d`。

## 八、安全与权限

沿用现有会话体系（HttpOnly Cookie + 服务端 token 哈希），补充：

- **注册接口防护**：公开接口限流（沿用现有登录限流机制）、用户名枚举防护（dummy-hash compare）、注册写审计
- **审批前隔离**：PENDING 用户任何业务接口都进不去（AuthGuard 校验 status，一处实现全局生效）
- **会话吊销**：禁用/重置密码/改角色时 `deleteMany` 该用户会话（修复审查报告问题）
- 角色收敛后权限判断简化为 `ADMIN` 常量 + `assigneeId = me` 对象级判断（顺带缓解权限模型死代码问题，但不重建 Permission 体系）
- 外网场景补充：Tailscale ACL 限制访问成员；frp 方案必须 HTTPS + 强密码 + 登录限流（现有）

权限矩阵：

| 功能 | ADMIN | USER(工程师) |
|---|---|---|
| 用户审批/管理 | ✅ | ❌ |
| 工单/借测/返修 全部数据 | ✅ | 自己创建 + 指派给自己的 |
| 客户/设备 查看 | ✅ | ✅ |
| 业务单创建与编辑 | ✅ | ✅（指派需 admin 或自己创建） |
| 分类字典/AI 设置 | ✅ | ❌ |

## 九、实施路线

| 阶段 | 内容 | 验收 |
|---|---|---|
| P1 数据层 | Prisma schema：User 扩展（status/部门等）、+Device/LoanOrder/LoanItem/RepairOrder/RepairEvent、Customer 扩展、Ticket.deviceId、编号规则、角色收敛迁移 | `migrate deploy` 通过 + 种子数据 |
| P2 后端 | auth 注册/审批流、users 管理接口（含会话吊销）、devices/loans/repairs 三模块 + 指派 + notifications 接通 + 逾期 cron | e2e 覆盖注册→审批→登录、三模块状态机、越权拒绝 |
| P3 前端 | 注册页、用户管理页改造、客户 360 页（Markdown）、设备台账、借测/返修页、导航/仪表盘 | Playwright 走通注册→审批→建单→指派→流转→归还 |
| P4 迁移入口 | 改造 `migration/import-v1.ts`：支持 customers/tickets 导入 + devices 从旧 `devices[]` 拆分（D3：保留入口，不执行导入） | 预检模式跑通旧 db.json |
| P5 部署 | Pi compose + Dockerfile 修正（审查报告 2.1/2.2）+ GHCR Actions + 备份脚本 | Pi 上外网/内网均可访问 |

## 十、非目标（明确不做，防臃肿）

- 微服务拆分、Redis、消息队列、Elasticsearch
- 移动端 App（Tailscale 下手机浏览器即可用）
- 客户自助门户（客户侧对接人只是 Contact 数据，无登录账号）
- 邮箱验证/手机验证码（内部系统，管理员人工审批即可）
- 细粒度 Permission 点体系（本期只做 ADMIN/USER 两级收敛）
- AI 新功能开发（D5：网关保留不动）
