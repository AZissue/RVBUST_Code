# 设备流转迁移暂停点

2026-09-16。基线提交 `21b4129`，本地分支 `feature/device-flow-html-full-21b4129`。

用户要求暂停并保存进度。此提交只是开发中的检查点，不是可交付版本；未上传 GitHub。

## 已完成的代码草稿

- 分析现有 React/NestJS/Prisma 路由、认证、设备相关模型和原 HTML 的主要数据与交互。
- 原 HTML 内置数据统计：1,102 条借测记录、75 条设备档案、37 条跟进；内置维修记录、图片和协议均为 0。浏览器后续产生的 localStorage 数据不在 HTML 文件内。
- 新增独立 `html_flow_*` 表迁移草稿与受当前登录保护的 API 草稿，不改旧业务表或旧 API。
- 从原 HTML 机械生成不含客户内置数据的 `apps/web/public/device-flow.html`，保留原布局及交互代码，并接入服务器数据/文件适配草稿。
- 新增数据导入脚本草稿和前端入口替换草稿。源 HTML 位于 `C:\Users\rvbust\Downloads\设备维修借测管理工具.html`，未复制其真实数据到仓库。

## 继续时必须完成

1. 先运行 Prisma generate、API/Web 构建，修复类型与运行问题。当前代码尚未构建或测试。
2. 审核并完善全量 HTML 功能，包括智能识别、PDF/OCR、Excel、图片和协议、跟进、回收站及并发保存。当前在线保存与附件代码只是草稿。
3. 检查旧设备页面和跨模块链接的兼容性；新入口尚未实际验收。
4. 仅在独立预览数据库执行新增迁移和 HTML 数据导入，验证数量、原值和空值。真实业务库未执行迁移，未导入数据。
5. 运行功能与回归测试，生成要求的 17 项正式迁移报告。未完成前不要宣称完整迁移成功，也不要上传 GitHub。

现有 `http://127.0.0.1:5177` 进程仍运行旧构建 API，并不代表本次草稿已生效。
