# CRM V2（crm_system_v2）架构审查报告

> 审查范围：apps/api（NestJS + Prisma）、apps/web（React + Vite）、工程化（Docker / e2e / 迁移工具）
> 审查日期：以仓库最新提交 `2076fa6` 为基准
> 说明：本文件未纳入 git 跟踪，仅供评审，确认后可删除或移出仓库。

## 一、总体评价

这是一个完成度明显高于一般内部项目的第一阶段模块化单体：

- **分层规整**：14 个 NestJS 模块标准三件套，Controller 只做路由、Service 承载业务与权限，DTO 校验覆盖度高（全局 `whitelist + forbidNonWhitelisted`）。
- **安全基线意识好**：会话 token 哈希存库（无需签名密钥）、bcrypt cost=12、登录限流、helmet、CORS 白名单、审计模块、迁移工具默认 dry-run 且绝不导入 V1 明文密码。
- **前端干净**：全库零 `any`、单一类型出口、统一 `useRemote` 数据抽象、无 XSS/内存泄漏面。
- **领域建模清晰**：Ticket / Work Item / Work Log 三者职责分离，状态机显式建模，快速记录的"AI 只整理不虚构"约定（`shared aiExtractionId` + DRAFT→CONFIRMED）设计得当。

主要债务集中在四块：**权限模型与实际 enforcement 脱节、审计不记失败事件、文件生命周期无补偿、列表无分页**。另外 Docker 链路有 2 个会导致数据丢失/泄密的实际缺陷，建议优先修。

---

## 二、必须优先处理的问题（高严重度）

### 2.1 上传卷挂载错位 —— 容器重建即丢附件【数据丢失】

- `apps/api/Dockerfile:19` CMD 先 `cd apps/api` 再启动，进程 cwd 为 `/app/apps/api`；`apps/api/src/main.ts:20` 用 `resolve('./uploads')` 相对 cwd 解析 → 实际上传目录是 `/app/apps/api/uploads`。
- 而 `docker-compose.yml:34` 的命名卷挂在 `/app/uploads`。**卷永远不会被写入**，容器重建后上传附件全部丢失。
- 修复（三选一）：compose 改挂 `/app/apps/api/uploads`；容器内显式 `UPLOAD_DIR=/app/uploads`；CMD 去掉 `cd`。

### 2.2 缺少 `.dockerignore` —— 构建上下文泄露 `.env` 与 `.runtime`【泄密】

- 仓库根目录无 `.dockerignore`。`docker compose build` 会把 `.env`（真实数据库/种子密码）、`.git`、`.runtime/pgsql`（便携 PostgreSQL 数据目录）全部送入构建上下文并进入缓存层。
- 修复：添加 `.dockerignore`，至少排除 `.env`、`.env.*`、`.git`、`.runtime`、`node_modules`、`uploads`。

### 2.3 权限模型是死代码，授权全靠角色名硬编码【架构债，最大】

- schema 定义了 `Role`/`Permission`/`RolePermission`，登录和 guard 也加载了 `permissions`（`auth.guard.ts:20`），但**没有任何 Guard/Service 消费 `AuthUser.permissions`**。
- 实际权限判断全部是 `user.role === 'customer'`（`access-policy.service.ts` 多处）、`@Roles('admin','support',...)` 装饰器、DTO 里 `@IsIn(['admin',...])`（`create-user.dto.ts:7`）。角色名字符串字面量散落 12+ 处。
- 后果：新增角色必须改代码且极易漏改；权限点（如 `tickets.manage`）与实际行为完全脱节，权限矩阵失去意义。
- 前端同样如此：后端已下发 `permissions: string[]`，前端 `types.ts:11` 也声明了，但全库无任何一处消费，判断全是 `user?.role === 'admin'`。
- 建议：短期至少把角色名抽成常量枚举并删掉误导性的 Permission 加载；正确方案是新增 `PermissionsGuard` 消费权限点，`access-policy` 改基于权限点判断，`@Roles` 逐步迁移。

### 2.4 审计拦截器只记成功请求【安全审计失效】

- `audit.interceptor.ts:13-24` 挂正常响应流，任何抛异常的请求（含 403 越权尝试）不落审计。登录失败在 `auth.service.ts` 单独补了，但其余模块的越权尝试完全不可见。
- 修复：在拦截器（或全局 ExceptionFilter）对异常流补写 `status:'failed'` + 状态码的审计记录。

### 2.5 文件上传无补偿，必然产生孤儿文件【磁盘膨胀】

- `files.controller.ts:19-26` multer 先写盘，之后 `files.service.ts:12` 才做权限校验——403/404 时文件已留在磁盘。
- attachment 创建与 `ticketEvent.create` 不在事务内（`files.service.ts:14-19`），后者失败留下孤儿。
- 全项目没有任何地方删除磁盘文件：工单删除只删 DB 行（`tickets.service.ts:131`），磁盘附件永留。
- 修复：register 包事务 + 失败 `unlink`；工单删除级联清理磁盘附件，或加定期清理任务。

### 2.6 前端会话过期无 401 兜底【体验主痛点】

- `apps/web/src/lib/api.ts:14-17` 对所有错误统一抛 `ApiError`，401 不清登录态、不跳 `/login`；`AuthContext.tsx:18-20` 只在挂载时校验一次会话。Cookie 过期后用户停留在"已登录"界面，每次操作只得到一条"请求失败"。
- 修复：api 层捕获 401 时触发登出并跳 `/login`，一处修改全局生效。

### 2.7 DTO 泄漏可信字段，破坏数据可信度【业务完整性】

- `CreateWorklogDto` 允许客户端直接传 `source`（可伪造 `AI_DRAFT`/`IMPORT`）和 `status`（可直接 `CONFIRMED` 跳过草稿确认流程）（`worklogs/dto/worklog.dto.ts:19-20`）。
- `CreateWorkItemDto.status` 可直接建 `COMPLETED` 事项。
- 修复：`source`/`status` 由服务端固定（`WEB`/`CONFIRMED`），状态流转只留在专用端点。

---

## 三、中等问题

### 后端

1. **外键引用裸奔**：`assigneeId`/`collaboratorIds`/`memberIds`/`ownerIds` 等系统性未校验存在性（如 `tickets.service.ts:77-78`、`customers.service.ts:52-59`），非法 UUID → FK 违反 → 未捕获的 500（应为 400）。建议统一"先查后写"或全局映射 Prisma 已知错误码。
2. **改密码/改角色/停用不吊销既有会话**（`users.service.ts:60`），且无定期清理过期 session 的任务（表只增不减）。管理员重置密码的常规预期是会话失效。
3. **限流 IP 可信链缺失**：`main.ts` 未配 `trust proxy`。部署在反代后限流键退化为 `username+代理IP`，攻击者刷 5 次即可让账号对所有真实用户锁定 15 分钟；直接暴露又可用 `X-Forwarded-For` 伪造绕过。两种部署形态都欠考虑。
4. **列表接口无分页**：tickets/worklogs/workitems `take: 500` 硬截断，customers/notifications/audit 或无上限或上限不合理，客户端无感知。第一阶段可接受，但需文档声明并列入第二阶段前补齐。
5. **通知系统是半成品**：除手工 POST 和 seed 外没有任何业务事件触发通知（工单分配/状态变更均不通知）；`NotificationsService.create` 是裸 `create`，重复 `dedupeKey` 直接 500 而非幂等返回。
6. **用户名枚举（时序泄露）**：`auth.service.ts:26` 用户不存在时跳过 `bcrypt.compare`，登录响应时间差可用于枚举有效用户名。应无条件对 dummy hash 执行一次 compare。
7. **无 `app.enableShutdownHooks()`**（`main.ts`），SIGTERM 时 Prisma 连接不会优雅关闭。

### 前端

8. **`tsconfig.app.json` 未启用 `strict`**，意味着 `strictNullChecks` 关闭——与全库零 `any` 的表象相反，类型安全是打折的。**成本最低收益最大的一项**：加 `"strict": true` 并修暴露的问题。
9. **全部列表无分页、前端全量拉取再内存过滤**（TicketsPage:22、WorklogsPage:16、AuditPage:8 等）。工作记录和审计日志是只增数据，几个月后首屏载荷失控。
10. **部分管理页无前端守卫**：Teams/Settings/Audit 页面内部完全没有角色检查（`SystemPages.tsx:11-26`、`AuditPage.tsx:7`），只靠侧栏隐藏入口——非 admin 直接输 URL 会看到后端 403 的原始错误页。数据不泄露（后端有 `@Roles('admin')`），但 UX 差且与后端 RBAC 不对应。
11. **WorkItemDrawer 切换事项不重渲染（真 bug）**：`WorkItemsPage.tsx:47` 的 `{selectedId && <WorkItemDrawer id={selectedId}>}`，A→B 时组件不 remount，字段全是 `defaultValue`（:64），显示上一个事项的旧值。修法：加 `key={selectedId}`（一行修复）。
12. **useRemote 请求竞态**（`useRemote.ts:9-11`）：快速切换筛选时慢的旧请求会后到覆盖新结果。加递增序号或 AbortController（约 5 行）。
13. **登录页硬编码默认账号 `admin`**（`LoginPage.tsx:9`），向任何打开登录页的人暗示管理员账号存在，生产必须移除。
14. **标签映射重复四份**：工单状态/优先级 label 在 Status.tsx、DashboardPage、WorkItemsPage、ReportsPage 各写一遍，已有漂移风险。抽 `lib/labels.ts`。
15. 新增用户角色是手输字符串（`UsersPage.tsx:18`），应改 select；`complete()` 等行内操作缺错误处理静默失败。

### 工程化

16. **nginx 未设 `client_max_body_size`**，默认 1m，与后端 `MAX_UPLOAD_MB=10` 冲突——任何 >1MB 附件经 Web 入口上传被 nginx 直接 413。另缺安全响应头、gzip、静态缓存策略。
17. **api/web 容器无健康检查**（postgres 有 `pg_isready`），API 已有 `/api/health` 端点，补上即可。
18. **无 CI/CD**：`.github/` 不存在，lint/单测/构建/e2e/`npm audit` 全靠手动。建议先加一条最小 Actions：`npm ci && lint && test && build`。
19. API 容器以 root 运行，镜像含全部 devDependencies 且启动时跑 `npx prisma migrate deploy`（可拆为独立 job）。
20. `migration/import-v1.ts:11` 深路径导入 `../apps/api/node_modules/@prisma/client/index.js`，Prisma 升级即可能断裂；`--apply` 无事务包裹，中途失败留下部分数据（幂等 upsert 可重跑缓解）。

---

## 四、低优先级 / 打磨项

- 审计 `entityType` 取 `path.split('/')[2]`，对 `/api/files/tickets/:id` 记成 `files` 而非 `tickets`；无 id 路由 `entityId` 为空（`audit.interceptor.ts:12`）。
- `tickets.service.ts:69 vs 82`：`data.number` 首次生成为无效代码。
- 命名不一致：`WorkitemsModule`/`WorktypesService`（小写 i）vs 目录 `workitems`、路由 `work-items`；`email || undefined` 与 `|| null` 语义不一（后者无法清空邮箱）。
- 上传类型校验只看客户端 mimetype，未校验扩展名白名单；`LIMIT_FILE_SIZE` 未映射，超大文件返回 500 而非 413。
- 客户创建工单 DTO 强制 `organizationId`（实际被 service 覆盖），接口冗余；登录密码最小 8 位 vs 建号最小 10 位不一致。
- 工单字段更新（非状态）不写 `TicketEvent`，内容编辑无历史轨迹。
- 根 `package.json` 的 `@playwright/test`/`concurrently` 用 `"latest"`，建议改 `^` 具体版本。
- Modal 无 Escape 关闭/焦点圈定；通知 popover 点外部不关闭、只在挂载时加载一次。
- `UsersPage` 的 `ManagedUser.role` 与 `types.ts` 的 `User.role` 形状不一致；`/reports` 与 `/stats` 冗余路由。
- compose 单网络无隔离（web 可直连 postgres）；文档小问题（README 写 PG 16+，compose 用 postgres:17）。

---

## 五、改进路线建议

**第一批（半天内，多为单行/小改动）**
1. `.dockerignore` + 上传卷路径修正（2.1、2.2）
2. `nginx.conf` 加 `client_max_body_size 10m;` + 基础安全头
3. `tsconfig.app.json` 加 `"strict": true`
4. api.ts 加 401 全局处理；移除登录页 `admin` 默认值
5. `WorkItemDrawer` 加 `key={selectedId}`；`useRemote` 加竞态保护
6. `CreateWorklogDto` 移除客户端可传 `source`/`status`
7. api/web 容器补 healthcheck

**第二批（1-2 天，安全加固）**
8. 审计补失败事件（ExceptionFilter）
9. 文件生命周期补偿（事务 + unlink + 级联清理）
10. 改密/改角色吊销会话 + 过期 session 清理任务
11. 外键存在性校验 + Prisma 错误码统一映射为 4xx
12. 登录 dummy-hash compare 防用户名枚举；`trust proxy` 按部署形态配置
13. 通知幂等返回 + 至少接通"工单分配/状态变更"两类业务事件

**第三批（架构收敛，进第二阶段前）**
14. 权限体系落地：PermissionsGuard + access-policy 基于权限点；前端 `hasPermission` helper + 管理页守卫
15. 列表分页（后端 page/pageSize + 前端翻页），优先 worklogs/audit
16. 最小 GitHub Actions 流水线，逐步加 Playwright e2e（补 tickets/files/customers 及对象级权限用例）
17. 抽 `lib/labels.ts` 合并标签映射；角色名抽常量枚举

---

## 六、结论

代码质量和工程素养在这个体量的内部系统里属于上乘，**不需要架构重构**。2.1/2.2 两个 Docker 缺陷和一个前端 drawer bug 是"立即修"级别；权限模型、审计失败事件、文件补偿是进第二阶段（拆服务/接 AI）前必须处理的三处架构债；其余均为定向修改。建议按第五节的路线分批执行。
