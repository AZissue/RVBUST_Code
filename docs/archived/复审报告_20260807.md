# MultiCameraCalibration 复审报告（第一轮修复后）

> 复审日期：2026-08-07 ｜ 复审人：Hermes（对照《代码整体审查与完善建议.md》§8 复审验收标准）
> 修复提交：`b7fb44f`（启动热修）→ `85a7004`（批1 数据正确性）→ `43de7fa`（批2/3 bridge 接线+模式B）→ `887595f`（批4/5 依赖+测试）
> 验证方式：逐提交 diff 核对 + 新增测试审读 + 全量测试实跑

---

## 一、复审结论

| 批次 | 范围 | 状态 |
|---|---|---|
| 批 1 | P0-1~3 数据正确性 + 真值测试 | ✅ **通过** |
| 批 2 | S1~S11 bridge 接线 | ✅ **通过** |
| 批 3 | 模式 B 状态一致性（P1-1~6） | 🔶 部分（算法层/数据层 P1-12~23 未做） |
| 批 4 | 测试补强 | 🔶 部分（工作流测试已补；L1/P1-26 未做） |
| 批 5 | 工程清理 | 🔶 部分（requirements 拆分完成；P2 卫生未做） |

**总评：核心数据正确性 + 模式 A/B 主流程链路已修复并验证通过，可以进入下一步开发。残留项不阻塞主流程使用。**

---

## 二、批 1 核对（✅ 通过，含实证）

| 项 | 修复方式 | 验证 |
|---|---|---|
| P0-1 pose_graph 方向 | `add_edge` 邻接表 from 存 T / to 存 T_inv，语义注释补齐；BA 初值改用 BFS 生成树（防 eye(4) 发散，附带改进） | `test_core.py [7f]`：用与复现实验相同的真值 [50,20,0] 验证正向/点变换/反向互逆 |
| P0-2 chain_stitcher 残留 | 配准失败与评估失败两处均 `nodes.pop(station_id)` | `test_chain_stitcher.py [5]`：5 标记拒绝→节点清理断言→重拍成功→变换误差 <0.01 |
| P0-3 标定板配色 | `_create_blob_detector` 支持 blob_color 参数，`_detect_pattern_robust` 遍历白圆/黑圆两种配色 × 多 gamma × 多规格 | `test_core.py [11b]`：白底黑圆 4x11 带梯度+噪声仿真图，44 圆 + RMS<0.1mm |

## 三、批 2 核对（✅ 通过）

| 项 | 修复确认 |
|---|---|
| S1 模式 A 状态机 | ✅ 连接成功调 `start_calibration(reference_id)`；标定质量门禁通过调 `start_scanning()` 并 `set_state("locked")`；`_work` 返回值检查 |
| S2 重拍当前 | ✅ `_station_id_from_timeline_index()` 映射（含 -1 语义） |
| S3 时间线错位 | ✅ 同一映射方法解决 |
| S4 重拍追加节点 | ✅ 重拍走替换路径（`on_recapture_done`） |
| S5 run_shell 崩溃 | ✅ 热修删除不存在的信号连接 |
| S6 set_busy 崩溃 | ✅ 热修移除 `_btn_net_cfg` 引用 |
| S7 参考相机 ID | ✅ `start_calibration` 同步 reference_id + `_on_multi_reference_changed` |
| S8 locked 状态 | ✅ 标定通过后 `set_state("locked")` |
| S9 批量扫描按钮 | ✅ bridge 实现批量槽（循环拍→拼接→写 PLY，失败返回明确信息） |
| S10 设备管理枚举 | ✅ 主窗口设备管理接真实枚举 |
| S11 假保存 | ✅ 真实现：merged.ply + error_report.json 双落盘（后台执行，返回 session_dir） |

## 四、批 3 核对（🔶 部分）

**已完成**：P1-1~6（时间线索引映射、重拍替换、capturing 门控防并发、撤销/删除/重拍误差重算、optimize_global 返回前后残差并刷新 3D、旧 UI main_window 适配 4 元组返回）

**残留（未修）**：
- P1-12/13：全局 BA 结果仍未接入拼接输出（"消除漂移"仍是摆设）；BA 残差量纲不均衡、`_se3_log` θ→π 除零
- P1-14：`CameraManager.capture()` 单拍不递增 frame_id（`capture_all` 已有 `_frame_counter`，单拍路径未接）
- P1-15：多帧标定全失败残留旧 pair_results；`_match_markers` 不过滤 NaN
- P1-16：删除/撤销机位不清理 pose_graph（chain_stitcher 场景已修，multi_cam 工作流场景未覆盖）
- P1-17：`calibrate_pair` 默认阈值 0.002 与 mm 数据不符
- P1-18/19/21：会话保存仍只存最新帧 / SessionManager 死代码 / 会话加载残缺（M6）
- P1-20：路径硬编码（offline_data、spec 中 RVC runtime 绝对路径）
- P1-22：`version.py` bump 对 "x.y.z-alpha" 仍会崩（`int("0-alpha")`）
- P1-23（M2）：预览定时器在模式切换/断开时不停

**UI 层其他残留**：M7 `_dirty` 永不置 True、M8 步骤回退无效、M9/M10 主线程合并/写盘（S11 已后台化，合并仍主线程）、M11 测试设备误连、M12 cleanup 竞态、M15/M16 新旧 UI 耦合、M17 预览主线程抓帧、M18 模式切换残留

## 五、批 4/5 核对（🔶 部分）

**已完成**：
- `test_workflows.py [1]`：模式 A 标定→扫描→保存/加载 JSON 完整链路
- `test_workflows.py [2b]`：模式 B 状态一致性回归（含全局优化残差 before/after、删除机位）
- requirements 拆分：`requirements.txt`（PyPI 可装核心依赖）+ `requirements-rvc.txt`（PyRVC 离线安装说明）

**残留**：
- L1：`smoke_test.py` 仍不导入 backend_bridge（grep=0）——**本轮 bridge 修复（S1~S11）无自动化拦截测试**，靠本次人工核对确认
- P1-26：pytest 误收 backup_* 未加排除配置
- P2 全部（性能后台化、新旧 UI 策略、垃圾文件清理、README 更新、meta.json 原子写、标定板 180°/RMS 门限）

## 六、全量测试实测结果（复审通过依据）

| 测试 | 结果 |
|---|---|
| test_core.py | ✅ exit=0 |
| test_chain_stitcher.py | ✅ exit=0 |
| test_workflows.py | ✅ exit=0 |
| test_station.py | ✅ exit=0 |
| test_integration.py | ✅ 逻辑全过（exit=127 为已知 Qt teardown 噪音） |
| test_ui.py | ✅ 逻辑全过（exit=127 为已知 Qt teardown 噪音） |
| smoke_test.py | ✅ [PASS] |

## 七、下一轮建议

1. **优先补 L1**：smoke_test 扩展 bridge 级用例（mock 相机走 S1/S2/S8 路径）——给本次修复上"保险"，防止回归
2. **算法层 P1-12/13/14/15/17**：BA 接入拼接、frame_id 单拍递增、NaN 过滤、阈值单位——改 core 时同步补测试
3. **数据层 P1-18~22**：会话完整保存、路径可配置、version.py 容错
4. **工程卫生 P2**：垃圾文件清理（backup_*、.venv-uiv2 661MB、dist 1.3GB、debug png）、README 更新
