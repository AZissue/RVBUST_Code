# PLAN.md — 执行计划

> 最后更新：2026-08-25
> 本文件回答「怎么做、按什么顺序、每步怎么算完成」。
> 阶段顺序由依赖关系决定；每阶段都有回退方案，git 历史可回溯。

## 阶段总览（依赖关系）

```text
阶段 0（仓库导入）
  └→ 阶段 1（工具骨架）
        └→ 阶段 2（项目文档）
              └→ 阶段 3（坐标转换）
                    ├→ 阶段 4（像素→3D）────────────┐
                    ├→ 阶段 5（棋盘格）──────────────┤
                    ├→ 阶段 6（鲁棒标定研究）────────┤
                    ├→ 阶段 7（标定一体化）──────────┤→ 阶段 11（打包发布）→ 阶段 12（合并 main）
                    ├→ 阶段 8（采集提质）────────────┤
                    ├→ 阶段 9（数据管理与交付）──────┤
                    └→ 阶段 10（相机与场景工具）─────┘
```

## 阶段 0：仓库导入与公约（已完成）
- 目标：v1.0 全功能导入 AICode monorepo（handeye-calib-tool 目录 + README + 根索引）。
- 涉及文件：AICode 根 README、项目 README、src/、tests/。
- 验证：`feat(handeye-calib-tool): import ...` 已提交；门禁（无密钥/大文件）通过。
- 依赖：无。
- 回退方案：提交历史可回溯；目录结构由 AICode 公约约束。

## 阶段 1：工具板块骨架 + 两点距离（已完成）
- 目标：顶栏「工具」入口、非模态 ToolsPanel（列表+内容区）、欧氏距离工具。
- 涉及文件：ui/ToolsPanel.*、logic/GeometryTools.h、tests/test_geometry_tools.*。
- DoD：
  - [x] 工具面板可打开并切换工具
  - [x] 欧氏距离单测全绿（5.000 mm 探针通过）
- 验证：单测绿；进程内探针（面板显示、5.000 mm）；分支提交 df248a6。
- 依赖：阶段 0。
- 回退方案：git 回退到 df248a6 之前。

## 阶段 2：项目文档体系（已完成）
- 目标：建立 AGENTS/PROJECT/PLAN/STATUS + SESSION_PROMPTS（4+1 份）。
- 涉及文件：AGENTS.md、PROJECT.md、PLAN.md、STATUS.md、SESSION_PROMPTS.md。
- DoD：
  - [x] 5 份文档齐全、无占位符、交叉引用一致
  - [x] STATUS 顶部有交接块；PLAN 每阶段有回退方案
- 验证：自查清单通过；文档同步到两处工作区。
- 依赖：无。
- 回退方案：git 历史保留旧三份版本。

## 阶段 3：坐标转换 / 走点验证工具（已实现，待 UI 实测）
- 目标：输入标定矩阵+安装方式+相机点+机械臂位姿 → 输出机器人基座点。
- 涉及文件：logic/TransformTools.h、logic/ToolInputParser.h、ui/ToolsPanel.cpp
  （坐标转换页）、tests/test_transform_tools.*、tests/test_tool_input_parser.*。
- DoD：
  - [x] TransformTools 23 项 + ToolInputParser 11 项单测全绿
  - [x] 真实数据复算：标定集 0.91mm / 验证集 0.78mm
  - [ ] 用户按测试清单 UI 实测（矩阵输入/安装方式/姿态格式/复制）
- 验证：两处工作区构建+ctest 全绿；真实数据复算。
- 依赖：阶段 2；用户确认输入/输出格式规格（已完成）。
- 回退方案：功能独立于采集链路；如实测发现约定问题，回退到「官方软件计算」
  并修正输入约定（FANUC WPR 顺序见 PROJECT ADR）。

## 阶段 4：2D 像素点 → 3D 点坐标（已实现，待联测）
- 目标：输入 2D 像素输出 3D 点；在线（拍照后 2D 图点击+3D 同步显示+回填
  相机目标点）与离线（会话文件夹+内外参可选）两种模式。
- 涉及文件：logic/PixelTo3DTools.h、logic/PlyPointReader.h、
  logic/CameraManager.*（grid/correspond 透出）、ui/Image2DView.*（pixelClicked）、
  app/MainWindow.cpp（在线取点）、ui/ToolsPanel.cpp（离线页）、
  tests/test_pixel_to_3d.*。
- DoD：
  - [x] 单测 11 项全绿（对齐索引/投影反查/CorrespondMap 反查/PLY 解析）
  - [x] 在线：2D 点击 → 3D 高亮 + 自动填入相机目标点
  - [x] 离线：会话数据可复算（对齐免内参；线扫用内参投影）
  - [ ] 真机联测：对齐与线扫两种模式各验证一轮
- 验证：两处构建+ctest 全绿；联测清单（见 STATUS 下一步）。
- 依赖：阶段 3。
- 回退方案：提交 4619dda 前为纯 SDK 直查；如投影反查引入问题，回退为
  「仅支持对齐数据」并保留 SDK 对应图路径。

## 阶段 5：棋盘格标定（未开工）
- 目标：新增棋盘格标定板识别（角点/位姿），复用「识别→保存→校验」链路。
- 涉及文件：logic/DetectionEngine.*、logic/HandEyeSDKBridge.*、
  ui/ModeSelector.*、tests/。
- 前置研究：RVC/HandEyeSDK 是否原生支持棋盘格；或引入 OpenCV 角点检测。
- DoD（待规格确认后细化）：与官方软件可比样本回归 + 单测 + 手动清单。
- 验证：与官方软件可比样本回归 + 单测 + UI 测试清单。
- 依赖：阶段 3。
- 回退方案：未开工无回退风险；引入 OpenCV 前先评估依赖体积。

## 阶段 6：相机好机器人差的标定优化（未开工，研究先行）
- 目标：机器人位姿读数误差大时仍能获得相对好的标定结果。
- 涉及文件：研究文档 + logic/（如鲁棒求解/离群剔除，必要时）。
- 内容方向：误差来源分析、采集策略（姿态分散/数据量）、RANSAC 离群剔除、
  加权/鲁棒求解（AX=XB 噪声）。
- DoD：方案评审通过 + 仿真/真实数据误差对比报告。
- 验证：仿真 + 真实数据复算（沿用标定集/验证集流程）。
- 依赖：阶段 3。
- 回退方案：研究产出不并入主流程，实现前需评审；可仅保留建议文档。

## 阶段 7：标定一体化（已实现，待联测；批量走点验证待补）
- 目标：程序内手眼标定（HandEyeSDK）+ 残差报告 + 批量走点验证。
- 涉及文件：logic/CalibrationService.*、app/MainWindow.cpp（计算按钮）、
  ui/SidePanel.*（标定结果 tab）、ui/ToolsPanel.cpp（手眼标定页）、
  tests/test_calibration_service.*。
- DoD：
  - [x] 单测全绿（位姿规范化/返回码映射）
  - [x] 主界面「计算」输出矩阵+总平均误差+逐组误差，可复制
  - [x] 工具面板离线页同数据可复算
  - [ ] 与官方 HandEyeSDK 同数据集结果逐元素一致（|Δ|<1e-6）
  - [ ] 批量走点验证（VC/VR/VT 误差统计表）
- 验证：两处构建+ctest 全绿；联测清单。
- 依赖：阶段 3。
- 回退方案：计算功能独立；如与官方结果不一致，回退为「官方软件标定 +
  程序只做残差展示」。

## 阶段 8：采集提质（机器人通信已实现；姿态引导/质检/板位姿待确认）
- 目标：机器人位姿自动读取（统一接口+Modbus TCP）；姿态引导/数据质检/
  板位姿可视化（待确认后实现）。
- 涉及文件：logic/RobotPose.*、ui/SidePanel.cpp（机器人通信卡）、
  app/MainWindow.cpp（接线）、tests/test_robot_pose.*。
- DoD：
  - [x] RobotPose 单测全绿（Float32/Int32/Int16/超时/未连接）
  - [x] 通信卡默认关闭、不影响现有链路；读取位姿填入卡片
  - [ ] 真机/Modbus 设备联调：寄存器布局与字节序按实际机器人确认
  - [ ] 姿态引导/质检/板位姿可视化：规格确认后补充 DoD
- 验证：两处构建+ctest 全绿；联测清单。
- 依赖：阶段 3。
- 回退方案：模块独立、默认关闭；如真机联调异常，撤掉通信卡 UI、保留
  RobotPose 模块（git 可回退到 4619dda）。

## 阶段 9：数据管理与交付（未开工）
- 目标：会话浏览器（筛选/重命名/合并/导出清单）、离线回放重识别、标定报告
  导出（PDF/HTML）、机器人专属位姿文件导出（FANUC .ls / KUKA src / UR script）。
- 涉及文件：logic/DataManager.*、新增会话加载器、ui/ 新页面、tests/。
- DoD（待规格确认）：单测（模板/合并逻辑）+ 手动清单。
- 验证：单测 + UI 测试清单。
- 依赖：阶段 3（合并多会话依赖会话数据格式稳定）。
- 回退方案：独立模块增量接入；报告导出可后置。

## 阶段 10：相机与场景工具（未开工）
- 目标：相机参数配置档案、2D 像素测量/标注、2D 相机内参标定（张正友法）。
- 涉及文件：ui/SettingsDialog.*、ui/ 新页面、logic/（内参求解）、tests/。
- DoD（待规格确认）：内参求解已知合成数据误差 + 手动清单。
- 验证：单测 + UI 测试清单。
- 依赖：阶段 3；内参标定为像素→3D 输入之一（弱依赖）。
- 回退方案：独立工具页；张正友实现前先验证与 OpenCV 结果一致性。

## 阶段 11：v2.0 打包与发布
- 目标：更新绿色打包脚本/使用说明；目标机实测。
- 涉及文件：打包脚本、README、build 产物（不入库）。
- DoD：
  - [ ] 依赖扫描无缺失
  - [ ] 目标机测试清单逐项通过（免装启动/连相机/保存回退/3D）
  - [ ] runtime 日志无异常
- 验证：目标机实测清单。
- 依赖：阶段 3、4、5、6、7、8、9、10 完成；真机/目标机可用。
- 回退方案：绿色包失败回退到上一可用包；依赖缺失按日志逐项补。

## 阶段 12：合并 main 与收尾
- 目标：tools-panel 分支合并回 main；README 状态 → v2.0；根索引同步。
- DoD：
  - [ ] 合并后 main 构建+单测绿
  - [ ] README 状态与根索引更新
- 验证：合并后构建+ctest；git 历史清晰。
- 依赖：阶段 11。
- 回退方案：合并前打标签；发现问题 revert 合并并保留分支。

## 阶段依赖关系表

| 阶段 | 依赖 | 完成后解锁 |
|---|---|---|
| 0 | 无 | 1 |
| 1 | 0 | 2 |
| 2 | 1 | 3 |
| 3 | 2 | 4-10 |
| 4-10 | 3 | 11 |
| 11 | 3-10 | 12 |
| 12 | 11 | — |

## 当前进行到

- 当前阶段：阶段 3/4/7/8 已实现待联测；阶段 5/6/9/10 未开工。
- 下一项任务：用户按三阶段联测清单实测（与 STATUS.md 交接块一致）。
