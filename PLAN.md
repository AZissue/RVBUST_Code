# PLAN.md — PointCloudSearch 执行计划

最后更新：2026-08-20

## 阶段总览与依赖

```
阶段0 环境与骨架 → 阶段1 画布/引擎 → 阶段2 节点逐个完善（当前）
                                          ↓
                              阶段3 形状与空洞节点
                                          ↓
                              阶段4 RVC 相机接入 → 阶段5 2D/深度学习
                                          ↓
                              阶段6 SDK 化与发布
```

## 阶段 0：环境与工程骨架 ✅ 已完成

- 目标：依赖安装与补丁（VTK 9.4 编译安装、PCLConfig 跳过 VTK、fmt stdext 兼容、
  `/utf-8`、运行时部署脚本）、模块划分、Graph DAG 与 9 个核心节点、app 框架。
- 验证：`ctest` 5/5 全绿；app 冒烟启动；双击 exe / VS F5 均可运行。

## 阶段 1：画布、执行引擎与核心交互 ✅ 已完成

- 目标：异步执行引擎（不卡 UI）、工具箱分类树 + 搜索、端口类型化与禁连、
  拖拽 / 点击连线、右键删除节点 / 断开连线、画布背景 4 种风格、节点选中联动
  3D 显示上游点云、方案 JSON 保存 / 加载、多视窗 + display3d、ROI 框选模块化、
  默认中文 + Dark、路径文件浏览、CLI（--smoke/--demo/--autoquit）。
- 验证：`ctest` 6/6 全绿（含拖拽连线、拖动跟随、删除、背景、中文标题用例）；
  E2E 冒烟：点云加载 → Box ROI → 保存点云 → 3D 显示一次跑通。

## 阶段 2：节点逐个完善（当前阶段 🔄）

- 目标：以「点云加载 → Box ROI → 保存点云」为基准流程，把中间节点逐个替换为
  其它节点（平面检测 / DBSCAN / 欧几里得聚类 / Z 过滤 / 体素 / 随机下采样 /
  ROI Crop 等）测试、完善每个组件的功能与联动。
- 验证：每个节点替换后跑一遍 E2E（加载 → 该节点 → 保存 / 3D 显示），
  检查输出数量、索引映射、参数面板、3D 显示；补对应单测；`ctest` 全绿。
- 依赖：阶段 0、1。

## 阶段 3：形状与空洞节点

- 目标：2D 形状（圆 / 矩形，平面点云上拟合）与 3D 形状（球 / 圆柱 / 长方体）
  检测节点；空洞检测（环形缺失区域，圆 / 方形）。
- 验证：合成点云单测数值断言（半径 / 圆心 / 内点占比）；GUI 3D 叠加显示形状；
  `ctest` 全绿。
- 依赖：阶段 1（ROI 用于裁剪输入）、阶段 2（平面检测为 2D 形状提供投影面）。

## 阶段 4：RVC 相机接入

- 目标：相机采集节点（枚举 / 打开 / 采图 / PointMap→mm 点云）、像素对齐 2D 显示、
  内外参可选、中文路径规避、Open 查重。
- 验证：真机联调；`--smoke` 链路（采集 → 保存）；2D/3D 叠加对齐目检。
- 依赖：阶段 1；RVC SDK 与真机 / 离线数据。

## 阶段 5：2D 识别与深度学习

- 目标：2D 视窗（来自相机 / 点云投影）、2D 检测节点与 3D 联动；
  深度学习语义分割 / 目标检测（YOLO / CNN 方案后定）。
- 验证：标注数据训练评估；GUI 2D→3D 联动演示。
- 依赖：阶段 4（像素对齐数据源）。

## 阶段 6：SDK 化与发布

- 目标：稳定 C++ SDK API + 示例工程 + 文档；打包部署（Qt/PCL/VTK/OpenCV 运行库）；
  客户集成指南。
- 验证：SDK 示例编译运行；安装包在干净机器可跑；`ctest` 全绿。
- 依赖：阶段 2~5 的节点库稳定。

## 变更管理约定

- 每完成一个阶段：先验证结果，再更新 STATUS.md。
- 需求 / 计划变化：同步更新 PROJECT.md（目标 / 约束 / 验收）与 PLAN.md（阶段 /
  验证 / 依赖）。

## 更新记录

- 2026-08-20：阶段 1 全部完成并通过验证（ctest 6/6、demo E2E：点云加载 → Box
  ROI → 保存点云 → 3D 显示）；进入阶段 2「节点逐个完善」，基准流程已跑通，
  下一步替换中间节点逐个测试。
