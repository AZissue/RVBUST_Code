# PLAN.md — 执行计划

> 最后更新：2026-08-25
> 本文件回答“怎么做、按什么顺序、每步怎么算完成”。
> 阶段顺序由依赖关系决定，不要跳过依赖未完成的阶段。

## 阶段总览（依赖关系）

```text
阶段 0 环境与工程骨架
  └─→ 阶段 1 画布 / 引擎与核心交互
        └─→ 阶段 2 节点逐个完善（当前 🔄）
              └─→ 阶段 3 形状与空洞节点
                    └─→ 阶段 4 RVC 相机接入 → 阶段 5 2D / 深度学习
                          └─→ 阶段 6 SDK 化与发布
```

## 阶段 0：环境与工程骨架 ✅ 已完成
- 目标：依赖安装与补丁（VTK 9.4 编译安装、PCLConfig 跳过 VTK、fmt stdext 兼容、
  `/utf-8`、运行时部署脚本）、模块划分、Graph DAG 与核心节点、app 框架。
- 涉及文件 / 模块：`cmake/`、`modules/core_data`、`modules/io`、`modules/pipeline`、
  `app/` 骨架
- 完成定义（DoD）：
  - [x] `ctest` 5/5 全绿
  - [x] app 冒烟启动；双击 exe / VS F5 均可运行
- 验证方式：`ctest -C Release`；启动 app 无崩溃。
- 依赖：无。
- 回退方案：回退到本阶段完成时的本地提交（git 基线之后的首个工程提交）。

## 阶段 1：画布、执行引擎与核心交互 ✅ 已完成
- 目标：异步执行引擎（不卡 UI）、工具箱分类树 + 搜索、端口类型化与禁连、
  拖拽 / 点击连线、右键删除节点 / 断开连线、画布背景 4 种风格、节点选中联动
  3D 显示上游点云、方案 JSON 保存 / 加载、多视窗 + display3d、ROI 框选模块化、
  默认中文 + Dark、路径文件浏览、CLI（--smoke / --demo / --autoquit）。
- 涉及文件 / 模块：`app/src/`（node_flow_widget、engine_runner、params_panel、
  point_cloud_view、roi_selector、main_window、main.cpp）、`modules/pipeline`
- 完成定义（DoD）：
  - [x] `ctest` 6/6 全绿（拖拽连线、拖动跟随、删除、背景、中文标题用例）
  - [x] E2E 冒烟：点云加载 → Box ROI → 保存点云 → 3D 显示一次跑通
- 验证方式：`ctest -C Release --output-on-failure`；`start.bat --demo <ply> --autoquit 5`。
- 依赖：阶段 0。
- 回退方案：回退到阶段 0 完成时的本地提交。

## 阶段 2：节点逐个完善（当前阶段 🔄）
- 目标：以「点云加载 → Box ROI → 保存点云」为基准流程，把中间节点逐个替换为
  其它节点（平面检测 / DBSCAN / 欧几里得聚类 / Z 过滤 / 体素 / 随机下采样 /
  ROI Crop 等）测试、完善每个组件的功能与联动。
- 涉及文件 / 模块：`modules/pipeline/src/nodes/core_nodes.cpp`、
  `modules/filters`、`modules/segmentation`、`modules/clustering`、
  `app/src/main_window.*`、`params_panel.*`、`point_cloud_view.*`、`roi_selector.*`
- 步骤：
  0. ✅ 按 PROJECT §8 统一约定落地批量语义，「加载 → Box ROI → 保存」三节点架构
     已打牢（PROJECT §9 前 7 项逐项提交）：load 文件夹批量 / 读取模式 +
     引擎分块执行（47bb5a6）→ display3d 多图层 + latest-wins + 视口预算
     （9189417）→ RoiBox label + 一帧多盒（d6ece80）→ save 零填充 / 多盒命名
     （d98b212）→ alignInputs + 批量 E2E（ce9323d）；
  1. 逐个节点跑 E2E（加载 → 该节点 → 保存 / 3D 显示），检查输出数量、索引映射、
     参数面板、3D 显示；
  2. 每个节点补单测；UI 交互按手动测试清单验证（见 STATUS.md 已知问题）；
  3. 新发现 BUG / 失败方案记录到 STATUS.md 对应表。
- 完成定义（DoD）：
  - [ ] 每个替换节点 E2E 通过（输出数量 / 索引映射 / 参数面板 / 3D 显示）
  - [ ] 每个节点对应单测通过；`ctest` 全绿
  - [ ] 冒烟通过
- 验证方式：`ctest -C Release --output-on-failure`；`start.bat --demo <ply> --autoquit 5`；
  手动测试清单。
- 依赖：阶段 0、1。
- 回退方案：单节点改动先本地提交再迭代，便于逐个回退；整体回退到阶段 1 完成时的
  本地提交。

## 阶段 3：形状与空洞节点
- 目标：2D 形状（圆 / 矩形，平面点云上拟合）与 3D 形状（球 / 圆柱 / 长方体）
  检测节点；空洞检测（环形缺失区域，圆 / 方形）。
- 涉及文件 / 模块：新增 `modules/shape/`（或并入 segmentation），
  `modules/pipeline/src/nodes/`、`app/src/point_cloud_view.*`（几何叠加显示）
- 完成定义（DoD）：
  - [ ] 合成点云单测数值断言（半径 / 圆心 / 内点占比）
  - [ ] GUI 3D 叠加显示形状
  - [ ] `ctest` 全绿
- 验证方式：合成数据单测；GUI 目检叠加。
- 依赖：阶段 1（ROI 用于裁剪输入）、阶段 2（平面检测为 2D 形状提供投影面）。
- 回退方案：形状算法模块与 UI 分开提交，算法失败不影响 UI 回退；整体回退到阶段 2
  完成时提交。

## 阶段 4：RVC 相机接入
- 目标：相机采集节点（枚举 / 打开 / 采图 / PointMap→mm 点云）、像素对齐 2D 显示、
  内外参可选、中文路径规避、Open 查重。
- 涉及文件 / 模块：新增 `modules/rvc/`（SDK 封装，独立于节点库）、
  `modules/pipeline/src/nodes/`、`app/src/viewport_manager.*`
- 完成定义（DoD）：
  - [ ] 真机联调：采集 → 保存链路跑通
  - [ ] 像素对齐 2D / 3D 叠加目检通过
  - [ ] 内外参缺失时给出明确提示
- 验证方式：真机 + 离线数据；`start.bat --smoke`。
- 依赖：阶段 1；RVC SDK 与真机 / 离线数据。
- 回退方案：SDK 封装与节点分开提交，失败回退到阶段 3 完成点，不影响节点库。

## 阶段 5：2D 识别与深度学习
- 目标：2D 视窗（来自相机 / 点云投影）、2D 检测节点与 3D 联动；深度学习语义分割 /
  目标检测（YOLO / CNN 方案后定，PROJECT.md 第 5 节非目标同步评估）。
- 涉及文件 / 模块：新增 `modules/vision/`（2D）、`modules/deep/`（模型推理）、
  `app/src/`（2D 视窗）
- 完成定义（DoD）：
  - [ ] GUI 2D → 3D 联动演示通过
  - [ ] 深度学习指标随方案确定后补充可测验收
- 验证方式：方案确定后定义；GUI 演示。
- 依赖：阶段 4（像素对齐数据源）。
- 回退方案：深度学习为独立模块，方案变更不影响主流程；回退到阶段 4 完成点。

## 阶段 6：SDK 化与发布
- 目标：稳定 C++ SDK API + 示例工程 + 文档；打包部署（Qt / PCL / VTK / OpenCV
  运行库）；客户集成指南。
- 涉及文件 / 模块：新增 `sdk/`（公共头文件 + 封装）、`examples/`、
  `cmake/deploy_runtime.cmake`
- 完成定义（DoD）：
  - [ ] SDK 示例编译运行
  - [ ] 安装包在干净机器可跑
  - [ ] `ctest` 全绿
- 验证方式：示例工程构建；干净机器安装冒烟。
- 依赖：阶段 2~5 节点库稳定。
- 回退方案：打包脚本与 SDK 头文件分离提交；回退到上一阶段完成点。

## 阶段依赖关系表
| 阶段 | 依赖 | 完成后解锁 |
|---|---|---|
| 0 | 无 | 1 |
| 1 | 0 | 2、3（部分）、4（部分） |
| 2 | 0、1 | 3 |
| 3 | 1、2 | 4 |
| 4 | 1、3 | 5 |
| 5 | 4 | 6 |
| 6 | 2~5 | — |

## 当前进行到
- 当前阶段：阶段 2（节点逐个完善）
- 本会话已完成：§8 统一约定确认；§9 前 7 项批量架构（load 批量 + 分块执行、
  display3d 多图层 / latest-wins / 视口预算 / 硬件档位、一帧多盒 + provenance、
  save 零填充 / 多盒命名、alignInputs + 批量 E2E）。
- 下一项任务：3D 视窗 ↔ 属性面板帧联动 + 运行拆分（全部/到当前）+ 参数中文/置灰 +
  分块累积已完成；继续 §9 第 8 项（输入端口 optional 标志），并用「点云加载 →
  替换节点 → 保存」逐个验证平面检测 / 聚类 / Z 过滤 / 下采样 / ROI Crop 的批量
  行为（§8.8），逐节点补单测；中间节点全量结果累积待用户确认后实现。
- 下会话优先级（2026-08-26 用户测试反馈，仅记录）：先修交互 bug（全选后 3D 清空、
  运行到当前后视窗不刷新、画布缩放回切、连线命中），再做新增需求（文件路径/文件夹
  互斥、平移/旋转工具、Box ROI 参数重构 [+/− 盒数量、单帧多盒、盒列表入对象属性]、
  显示数据类型多选筛选、运行交互提示与禁用规则），详见 STATUS 交接块「下会话待办」。

## 变更管理约定
- 每完成一个阶段：**先验证结果，再更新 STATUS.md**，并把 PLAN 对应阶段标记 ✅。
- 需求 / 计划变化：同步更新 PROJECT.md 与 PLAN.md（阶段 / 验证 / 依赖）；
  STATUS.md 只记录“现在到哪了”。
- 本地提交照常；push 需用户明确要求。
